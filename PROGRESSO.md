# PROGRESSO — Fundação do projeto + RF01 (Autenticação e Perfis)

## O que foi implementado

### 1. PRD versionado no repositório
- `docs/PRD.md` — cópia integral do PRD, conforme recomendado na seção 13.2 ("o PRD deve viajar com cada sessão").

### 2. Modelo de dados completo (seção 9 do PRD)
- Migration `supabase/migrations/20260905120000_init_schema.sql`, aplicada diretamente no banco Lovable Cloud (Supabase) do projeto.
- Todas as tabelas do V1 (MVP) + `expenses` (Fase 2, incluída agora para não exigir retrabalho estrutural depois): `profiles`, `products`, `purchases`, `purchase_items`, `sales`, `sale_items`, `sale_returns`, `orders`, `settings`, `expenses`.
- Enums para `role`, gênero, forma de pagamento, status de venda/encomenda, categoria de despesa.
- `settings` seedada com `default_commission_percent` (5), `low_stock_threshold` (1), `stale_product_days` (30), `edit_window_hours_seller` (2).
- Índices nas colunas mais consultadas (FKs, `active`, datas, `status`).

### 3. Row Level Security (RF02 / RF10)
- RLS habilitado em todas as tabelas.
- `products`, `purchases`/`purchase_items` e a coluna `commission_amount` de `sales`: acesso à tabela base restrito a `master` (via função `is_master()`, `security definer`, evita recursão de RLS).
- Como RLS do Postgres é por linha (não por coluna), o vendedor nunca lê custo/margem/comissão porque essas tabelas ficam bloqueadas para ele na base — em vez disso, ele consulta as views `products_catalog_v` e `sales_seller_v`, que expõem só as colunas seguras. Essas views são "owner-bypass" (criadas sem `security_invoker`, then o dono da migration ignora a RLS da tabela base) e replicam manualmente o filtro de linha necessário. Isso satisfaz o critério de aceite do RF10 mesmo contra chamadas diretas à API.
- `sales`/`sale_items`: vendedor grava e corrige as próprias vendas na tabela base dentro da janela de edição (RF13, via `seller_edit_window_hours()`); master sem limite.
- `orders`: leitura/edição compartilhada entre os dois papéis (Tela 9); exclusão restrita ao master (pedido do LGPD, seção 8).
- `settings`: a linha `default_commission_percent` é ocultada do vendedor por RLS; as demais (limite de estoque baixo, etc.) ficam visíveis.

### 4. Autenticação e redirecionamento por papel (RF01)
- `src/routes/login.tsx` — Tela 1: e-mail/senha, "Esqueci minha senha" (via `resetPasswordForEmail`), estados de vazio/erro/carregando conforme especificado.
- `src/components/require-role.tsx` — guarda de rota: sem sessão → `/login`; papel errado para a rota → redireciona para a home do papel correto (bloqueia acesso direto via URL, RF02).
- `src/routes/master/route.tsx` (layout do master, com `DashboardShell` + nav) e `src/routes/vendedor.tsx` (Tela 3) — shells dos dashboards, com dados reais (contagem de produtos, estoque baixo, encomendas em aberto para o master; catálogo com busca para o vendedor via `products_catalog_v`).
- `src/lib/auth.ts` — hooks `useSession`/`useProfile`/`useAuth` (React Query + `onAuthStateChange`).

### 5. Bootstrap do primeiro usuário — decisão técnica
O PRD diz que "não há autocadastro público — novos usuários só são criados por um master" (Tela 1), mas não define como nasce o **primeiro** master. Decisão adotada: um trigger em `auth.users` (`handle_new_user`) atribui `role = 'master'` automaticamente ao primeiro usuário já criado no sistema; os seguintes nascem `vendedor` por padrão (ou o papel vier em `raw_user_meta_data.role`, usado futuramente pela criação administrativa da Tela 11). Uma função pública `needs_bootstrap()` permite à tela de login oferecer "Criar conta master" somente enquanto não existir nenhum usuário — depois disso, a opção desaparece e a criação de vendedores/masters passa a ser exclusiva da Tela 11 (RF02, ainda não implementada).

### 6. Identidade visual (seção 14)
- `src/styles.css` — paleta bone/surface/ink/wine/amber/hairline + cores funcionais (success/warning/danger) convertidas para oklch e mapeadas nos tokens semânticos do shadcn (`background`, `primary`, `accent`, etc.), além de expostas como tokens de marca (`bg-wine`, `text-amber`, etc.).
- Tipografia Fraunces (display) + IBM Plex Sans (corpo), raio pequeno (6px) em botões/inputs, algarismos tabulares (`.tabular`) para colunas numéricas.
- Paleta de gráficos alinhada à seção 14.5 (`chart-1` = wine, `chart-2` = amber).
- **Padrão do item de menu ativo** (`src/components/dashboard-shell.tsx`): `bg-sidebar-accent/10 text-amber`, seguindo a seção 14.2 à risca ("amber... item de menu ativo, badges neutros" — não `wine`, reservado para ações principais/marca). Esse é o padrão a repetir quando Compras, Estoque, Vendas, Encomendas, Relatórios e Usuários (hoje placeholders "Em breve" em `DashboardShell`) ganharem suas telas e virarem links reais.

### 7. Tipos TypeScript
- `src/integrations/supabase/types.ts` reescrito manualmente a partir da migration (sem CLI do Supabase disponível neste ambiente). **Atenção:** ao alterar o schema, atualizar este arquivo junto — não há geração automática configurada.

### 8. Refino de RLS: `is_own_sale()` (migration `20260906090000_sale_items_is_own_sale.sql`)
As 4 políticas de `sale_items` (select/insert/update/delete) repetiam a mesma
subquery `exists (select 1 from sales s where s.id = sale_id and s.seller_id =
auth.uid() ...)`, variando só a checagem da janela de correção (RF13). Foi
extraído um único helper:

```sql
public.is_own_sale(p_sale_id uuid, p_within_edit_window boolean default false)
```

`select`/`insert` chamam `is_own_sale(sale_id)` (sem checar janela);
`update`/`delete` chamam `is_own_sale(sale_id, true)` (exige estar dentro de
`seller_edit_window_hours()`). Reduz duplicação e centraliza a regra "esta
venda é minha" em um único lugar, caso ela mude no futuro.

### 9. Padrão obrigatório: leitura pós-insert/update em `sales` (vendedor)
A tabela base `sales` só permite `SELECT` para `is_master()` (item 3, acima) —
é assim que `commission_amount` fica escondido do vendedor mesmo em chamada
direta à API (RF10). Isso tem uma consequência que **quem implementar RF07
(Registrar Venda) e RF13 (correção) precisa seguir**:

- O Postgres aplica a política de `SELECT` também ao `RETURNING` de um
  `INSERT`/`UPDATE`/`DELETE`. Como o `supabase-js` ativa `RETURNING` sempre
  que `.select()` é encadeado após `.insert()`/`.update()`/`.delete()`, um
  vendedor que faça `supabase.from("sales").insert(payload).select()` recebe
  `data: []` (silencioso, sem erro) mesmo com o insert tendo funcionado —
  porque a linha inserida não passa na política de `SELECT` da tabela base.
- **Padrão a seguir:**
  1. Gerar o `id` da venda no cliente com `crypto.randomUUID()` antes do
     insert, para não depender do valor de volta do banco.
  2. Ao inserir/atualizar/excluir em `sales` como vendedor, **não encadear
     `.select()`** — só checar `error`. Isso mantém o `Prefer: return=minimal`
     (comportamento padrão do `supabase-js` sem `.select()`), que não aciona a
     política de `SELECT`.
  3. `sale_items` não tem essa restrição (RLS permite ao vendedor ler os
     próprios itens), então `.select()` pode ser encadeado normalmente ali.
  4. Sempre que o vendedor precisar reler dados de `sales` depois (ex: listar
     vendas recentes para permitir a correção dentro das 2h — RF13), consultar
     a view `sales_seller_v` (já filtrada por `seller_id = auth.uid()` e sem
     `commission_amount`), nunca a tabela base `sales`.

### 10. RF03 — Catálogo de Produtos (Telas 4 e 5)
- `src/routes/master.tsx` virou um layout de pasta (`src/routes/master/route.tsx` + `index.tsx`), para acomodar as novas sub-rotas — o item de nav "Catálogo" agora é um link real; os demais continuam como placeholder ("Em breve") até suas telas existirem.
- `src/routes/master/catalogo/index.tsx` (Tela 4): grade de produtos com imagem, nome, marca, ml, estoque, custo e preço de venda; busca por nome/marca; filtro por gênero; ordenação padrão por estoque baixo primeiro; oculta inativos por padrão com um toggle "Mostrar inativos" (badge visual "inativo" quando exibidos); estados de vazio/erro/carregando.
- `src/routes/master/catalogo/novo.tsx` e `.../$productId.tsx` (Tela 5): cadastro e edição usando o mesmo `src/components/product-form.tsx` — nome, marca, gênero, ml e markup obrigatórios; descrição opcional; upload de imagem para o Storage; toggle "Produto ativo" visível só na edição (todo produto novo nasce ativo). Custo e preço de venda **não** são definidos aqui, conforme RF04/RF06 — ficam com o valor padrão (`0`) até a primeira compra.
- Bucket de Storage `products` criado via migration `20260906120000_product_images_storage.sql`: leitura pública (a imagem não é dado sensível — aparece também no catálogo do vendedor), escrita/atualização/exclusão restritas a `is_master()`.
- **Padrão de geração de `id` client-side**, já usado para `sales` (seção 9), reaproveitado aqui: o `id` do produto é gerado com `crypto.randomUUID()` antes do insert, para poder montar o caminho da imagem no Storage (`{id}/{timestamp}.{ext}`) antes de a linha existir na tabela.
- Dashboard do master (Tela 2) ganhou um link real "Cadastrar produto" no estado vazio, agora que o Catálogo existe.

### 11. RF04/RF06 — Registrar Compra (Tela 6)
- Implementado como RPC transacional `public.confirm_purchase()` (migration `20260907090000_confirm_purchase.sql`), não como inserts diretos do front: uma compra grava a linha em `purchases`, uma linha por item em `purchase_items` e atualiza `stock_quantity`/`current_unit_cost_brl`/`current_sale_price`/`last_purchase_date` de cada produto — tudo em uma única transação de banco, garantindo que não existe estado intermediário (ex.: estoque atualizado sem o registro da compra, ou vice-versa).
- `security invoker` (padrão, não `security definer`): a função roda com o papel de quem chama, então depende só das policies `purchases_all_master`/`purchase_items_all_master`/`products_update_master` já existentes — nenhuma policy nova foi necessária. `is_master()` é checado explicitamente no início da função como segunda barreira (defesa em profundidade), já que Compras é uma tela exclusiva do master (RF02).
- Rateio de frete **por quantidade**, conforme seção 7/RF04: `frete_por_unidade = frete_total ÷ quantidade_total_de_itens_da_compra` (soma de todos os produtos da nota), não por valor do item. Fórmula de custo: `custo_unitário_BRL = (preço_unitário_USD × taxa_câmbio) + frete_por_unidade`.
- Preço de venda sugerido (`custo_unitário_BRL × (1 + markup_%_do_produto)`) é calculado e exibido no front (`src/components/purchase-form.tsx`) para cada item, com campo editável ao lado — o master pode aceitar a sugestão ou digitar outro valor antes de confirmar. O valor final enviado ao RPC (sugerido ou ajustado) é o que grava em `current_sale_price`, único por produto (RF06), mesmo com lotes de compra diferentes em estoque.
- Tela em formato de tabela/planilha (`src/components/purchase-form.tsx`), com colunas numéricas alinhadas à direita, conforme a diretriz visual da Tela 6; suporta múltiplos produtos na mesma compra (adicionar/remover linhas).
- Nav "Compras" (`src/routes/master/route.tsx`) e o atalho "Registrar Compra" no Dashboard Master (`src/routes/master/index.tsx`, Tela 2, elemento 2) agora apontam para `/master/compras/nova`. O atalho "Registrar Venda" (elemento 3 da mesma tela) fica para quando o RF07 existir.
- `src/integrations/supabase/types.ts` ganhou a assinatura de `confirm_purchase` em `Functions`.

### 12. RF04 — correção: produto duplicado na mesma compra
- **Problema encontrado:** era possível selecionar o mesmo produto em duas ou mais linhas da mesma compra (Tela 6). `confirm_purchase()` processa os itens em sequência e faz um `update` em `products` por linha — a quantidade somava corretamente, mas `current_unit_cost_brl`/`current_sale_price` acabavam ficando com o valor só da última linha processada para aquele produto, silenciosamente (sem erro, sem aviso).
- **Correção no frontend** (`src/components/purchase-form.tsx`): `handleProductChange()` intercepta a seleção antes de aplicá-la — se o `product_id` já estiver em uso em outra linha da mesma compra, a seleção é rejeitada com a mensagem "Este produto já foi adicionado nesta compra — ajuste a quantidade na linha existente em vez de duplicar", em vez de somar as linhas silenciosamente. Correção acompanhante: o `<Select>` do produto passou a receber `value={item.productId}` incondicionalmente (antes só passava `value` quando havia produto selecionado) — sem isso, o campo nascia "não controlado" no Radix e a rejeição não revertia visualmente o item exibido no dropdown, mesmo com o estado da aplicação correto por baixo.
- **Correção no backend** (migration `20260907100000_confirm_purchase_no_duplicates.sql`, substitui a função de `20260907090000_confirm_purchase.sql`): `confirm_purchase()` agora compara `count(*)` com `count(distinct product_id)` em `p_items` logo após validar que há ao menos um item, e faz `raise exception` se houver duplicidade — segunda barreira, mesmo padrão de defesa em profundidade já usado para `is_master()` nesta função (a RPC não confia cegamente na validação do cliente).
- Fórmula de custo, rateio de frete, cálculo de preço sugerido e RLS não foram alterados — só a validação de duplicidade foi adicionada.

### 13. RF07/RF06 — Registrar Venda (Tela 8)
- Implementado como RPC transacional `public.confirm_sale()` (migration `20260907110000_confirm_sale.sql`): uma venda grava a linha em `sales`, uma linha por item em `sale_items` e debita `stock_quantity` de cada produto — tudo em uma única transação, mesmo racional de atomicidade do `confirm_purchase()` (seção 11).
- **Diferença deliberada em relação a `confirm_purchase()`: aqui a função é `security definer`, não `security invoker`.** Quem chama normalmente é o vendedor, que (por desenho, RF10) não tem `UPDATE` em `products` nem `SELECT` na tabela base `sales` — a seção 9 já documenta que isso quebraria um `insert().select()` direto. Abrir uma policy de `UPDATE` em `products` para o vendedor exigiria também impedir que ele altere `current_unit_cost_brl`/`markup_percent` pela mesma via, o que RLS por linha não resolve sozinho. Em vez disso, `confirm_sale()` concentra num único lugar auditável a única coisa que o vendedor precisa além da sua RLS normal (debitar estoque), e a função nunca aceita do cliente nem `seller_id` (sempre `auth.uid()`) nem o preço de venda (sempre o `current_sale_price` vigente do produto, lido no servidor) — a mesma lógica de "a RPC não confia no cliente" já usada para o custo em `confirm_purchase()`, aqui aplicada ao preço de venda. Isso também elimina de raiz o problema do RETURNING da seção 9: a função nunca faz `insert ... returning` em `sales`/`sale_items`, só `return v_sale_id` (um escalar, não uma linha lida da tabela).
- Novo helper `public.default_commission_percent()` (mesmo estilo de `seller_edit_window_hours()`, já existente): centraliza a leitura de `settings.default_commission_percent` — normalmente invisível ao vendedor via API direta (RLS), mas lida internamente pela função (que roda como `security definer`) só para calcular `commission_amount`, nunca para expor o percentual em si.
- Fórmula: por item, `subtotal = current_sale_price × quantidade × (1 − desconto_%/100)`; `commission_amount = soma_dos_subtotais × percentual_de_comissão / 100` — conforme RF06/critério de aceite. O front (`src/components/sale-form.tsx`) nunca recebe nem exibe `commission_amount`.
- Checagem de estoque insuficiente **por produto agregado entre linhas**, não por linha isolada: como vender o mesmo produto em duas linhas não sobrescreve nada (diferente do bug de compra da seção 12 — aqui cada linha só soma ao `subtotal`/decrementa estoque, não hà campo que uma linha sobrescreva o valor da outra), não foi necessário bloquear produto duplicado; mas o front soma a quantidade pedida por `product_id` entre todas as linhas antes de comparar com o estoque disponível, senão duas linhas de 2 unidades cada pareceriam válidas isoladamente mesmo estourando um estoque de 3. O backend repete a checagem por linha, sequencialmente (`update products set stock_quantity = stock_quantity - qtd where stock_quantity >= qtd`), que já é correto para múltiplas linhas do mesmo produto porque cada iteração lê o estoque já decrementado pela linha anterior — segunda barreira, mesmo padrão de defesa em profundidade.
- Preço de venda e estoque disponível vêm de `products_catalog_v` (RF10) — o formulário do vendedor nunca lê a tabela base `products`; o preço não é editável na tela, só o desconto por item (RF06: desconto pontual por produto, recalcula o subtotal a partir do `current_sale_price` vigente).
- Forma de pagamento obrigatória (espécie/débito/crédito à vista/crédito parcelado); vendedor responsável não aparece como campo — já é o usuário logado, mostrado no cabeçalho do shell ("Olá, {nome}"), preenchido automaticamente como `auth.uid()` dentro da função.
- Estados da Tela 8: botão "Concluir Venda" desabilitado enquanto forma de pagamento, produto ou quantidade (agregada) inválida faltarem; após confirmar, tela de sucesso com o total e botão "Nova Venda" que reseta o formulário (a PRD pede confirmação + opção de nova venda, diferente da Tela 6 que navega para o catálogo).
- Reestruturado `src/routes/vendedor.tsx` (arquivo único) em pasta (`src/routes/vendedor/route.tsx` layout + `index.tsx` Tela 3), mesmo padrão já usado para `master` (seção 10), para acomodar a nova sub-rota `src/routes/vendedor/vendas/nova.tsx` (Tela 8). O botão "Registrar Venda" da Tela 3 deixou de ser um `toast("Em breve")` e agora navega para lá.
- **Fora de escopo desta entrega, deliberadamente**: o item "Vendas" do nav do master (`src/routes/master/route.tsx`) continua placeholder. A Tela 8 do PRD é explicitamente rotulada "(Vendedor)" — uma eventual listagem/histórico de vendas para o master é mais próxima do RF09 (Relatórios) e fica para quando essa RF for construída, junto com a pendência equivalente já registrada para compras (item 6 da lista de pendências).
- `src/integrations/supabase/types.ts` ganhou as assinaturas de `confirm_sale` e `default_commission_percent` em `Functions`.

### 14. RF07 — correção pós-revisão da PR #4: desconto sem validação de faixa no backend
- **Problema encontrado (revisão contra o PRD):** `confirm_sale()` aceitava `discount_percent` do payload sem validar faixa — só o front travava 0-100. Como é uma RPC pública (`grant execute ... to authenticated`), um vendedor chamando o RPC diretamente (fora do front) podia mandar `discount_percent` negativo (infla o preço cobrado do cliente, já que `subtotal = preço × quantidade × (1 − desconto/100)`) ou maior que 100 (gera subtotal negativo, reduzindo o total e a própria comissão da venda). Quebra a mesma filosofia já aplicada ao preço e ao `seller_id` nesta função: "a RPC não confia no cliente".
- **Correção no backend** (migration `20260908090000_confirm_sale_validate_discount.sql`, substitui a função de `20260907110000_confirm_sale.sql`): dentro do loop de itens, `confirm_sale()` agora faz `raise exception` se `discount_percent < 0 or discount_percent > 100` — mesmo padrão de defesa em profundidade já usado para duplicidade em `confirm_purchase()` e para quantidade inválida nesta própria função. Não foi necessária mudança no front (`sale-form.tsx`), que já limitava o input a 0-100 (`min="0" max="100"`); esta é só a segunda barreira que faltava no servidor.
- **`sales.status`, também revisado nesta correção:** a coluna já tinha `default 'concluida'::sale_status` desde a migration inicial (`20260905120000_init_schema.sql`) — confirmado via `information_schema.columns` diretamente no banco, nenhum `alter table` foi necessário. Ainda assim, o `insert` em `confirm_sale()` passou a setar `status = 'concluida'` explicitamente, por clareza (RF14/devolução depende desse valor estar correto desde a criação da venda).
- Fórmula de custo/preço, rateio de frete, cálculo de comissão e RLS não foram alterados — só a validação de desconto foi adicionada e o `status` passou a ser explícito.

### 15. RF13/RF14 — Correção/estorno de lançamentos (exclusão) e Devolução de Venda (Tela 12)
- **Escopo de RF13 nesta entrega — decisão técnica:** ao contrário da Tela 5 (produto), o PRD não descreve uma tela dedicada de "edição" de compra/venda, e os critérios de aceite de RF13 (linha 511) testam só reversão de estoque em **exclusões** ("uma exclusão de venda repõe a quantidade ao estoque; uma exclusão de compra remove a quantidade do estoque"). Por isso RF13 foi implementado como duas RPCs de exclusão (`delete_sale()`, `delete_purchase()`); "editar" continua possível na prática como excluir + relançar pelas telas já existentes (Tela 6/Tela 8), respeitando a mesma janela de permissão — evita construir uma segunda tela de formulário (edição) sem respaldo explícito no PRD.
- **`public.delete_sale(p_sale_id)`** (migration `20260909090000_correction_return.sql`) — `security definer`, mesmo motivo de `confirm_sale()` (seção 13): o vendedor não tem `UPDATE` em `products`. A checagem de permissão reaproveita o helper `is_own_sale(p_sale_id, true)` já existente (seção 8) — `is_master() or is_own_sale(...)` — nunca concede mais acesso do que a RLS de `sales_delete_own_within_window_or_master` já concederia para o `DELETE` em si. Reverte o estoque somando por produto **antes** de aplicar o `update` (`group by product_id` numa subquery), evitando o mesmo bug de sobrescrita já corrigido em `confirm_purchase()` (seção 12) para o caso de duas linhas do mesmo produto na venda. Bloqueia a exclusão se a venda já teve qualquer item devolvido (`sale_items.returned_quantity > 0`) — decisão de escopo: evita reconstruir a contabilidade de uma devolução já registrada, o histórico em `sale_returns` seria órfão.
- **`public.delete_purchase(p_purchase_id)`** — `security invoker`: `purchases`/`purchase_items` são 100% restritas a master em toda a RLS (`purchases_all_master`), não existe caso de vendedor aqui, mesmo racional de `confirm_purchase()` (seção 11). Reverte o estoque por item (`purchase_items`) e bloqueia (`raise exception`) se a reversão deixaria algum produto com estoque negativo (RF13, critério de aceite) — ex.: produto já vendido depois da compra.
- **`public.return_sale_items(p_sale_id, p_items)`** (RF14, Tela 12 — exclusiva do master) — `security invoker`: o master já tem RLS completa em `sales`/`sale_items`/`products`/`sale_returns`, não precisa elevar privilégio (mesmo racional de `confirm_purchase()`). Para cada item devolvido: valida que o item pertence à venda informada, que a quantidade devolvida não excede o saldo disponível (`quantity - returned_quantity`), incrementa `returned_quantity` e `products.stock_quantity`, e grava uma linha em `sale_returns` (histórico com motivo, quem e quando). Ao final, recalcula `sales.total_amount`/`commission_amount` e `status`:
  - **Estorno proporcional (RF14):** o valor estornado por unidade devolvida é recalculado com a mesma fórmula do item na venda original (`unit_sale_price × (1 − discount_percent/100)`), não um valor médio — preserva o desconto que foi aplicado àquele item especificamente.
  - **Comissão:** a proporção estornada usa a **taxa efetiva da própria venda** (`commission_amount ÷ total_amount` da venda, calculada antes do estorno), não o `default_commission_percent` atual em `settings` — importante porque o percentual configurado pode ter mudado desde a venda original; RF14 pede estorno proporcional "à venda", não à configuração vigente.
  - **Status:** `totalmente_devolvida` se todos os itens da venda estiverem com `returned_quantity = quantity`; `parcialmente_devolvida` se algum item tiver `returned_quantity > 0` sem estar tudo devolvido; mantém o status atual caso contrário.
- **Frontend:**
  - `src/components/seller-recent-sales.tsx` — lista compacta ("Minhas últimas vendas", últimas 10) no dashboard do vendedor (`src/routes/vendedor/index.tsx`), usando `sales_seller_v` (seção 9). Botão "Excluir" habilitado só quando `status = 'concluida'` e dentro de `settings.edit_window_hours_seller` (lido diretamente da tabela, visível ao vendedor por RLS — seção 3); fora da janela ou já devolvida, o botão fica desabilitado com `title` explicando o motivo. Não é a listagem completa de histórico de vendas (pendência já registrada, deferida a Relatórios/RF09) — só o necessário para RF13 no lado do vendedor.
  - `src/components/sale-return-panel.tsx` (Tela 12) — busca por vendedor (select), intervalo de datas e produto (texto, filtrado no cliente após carregar os itens das vendas do resultado); lista as vendas encontradas (até 50, mais recentes primeiro) com um resumo (valor, vendedor, forma de pagamento, data, status); ao expandir uma venda, mostra os itens (vendido/já devolvido/disponível), campo de quantidade a devolver por item (limitado ao saldo disponível), motivo opcional, resumo do impacto (valor a estornar + ajuste de comissão, calculado no cliente com a mesma fórmula do backend, só para preview antes de confirmar) e o botão "Confirmar Devolução". Como o master não tem limite de tempo para corrigir lançamentos (RF13), a mesma linha expandida também tem "Excluir venda" (desabilitado se a venda já tiver qualquer devolução, espelhando a regra do backend). Consultas em tabelas base (`sales`, `sale_items`, `products`, `profiles`) direto — sem depender de views — porque esta tela é exclusiva do master, que já tem RLS completa nelas.
  - Novo item de nav do master: "Devolução de Venda" → `/master/vendas/devolucao` (`src/routes/master/route.tsx`), substituindo o placeholder "Vendas". **Ainda não é uma listagem completa de histórico de vendas** (filtra por vendedor/data/produto para fins de correção/devolução, não pagina nem mostra todas as vendas) — a listagem completa continua pendência, deferida a Relatórios/RF09 (mesma pendência já registrada nas seções 11/13, só com a nomenclatura do nav atualizada).
- `src/integrations/supabase/types.ts` ganhou as assinaturas de `delete_sale`, `delete_purchase` e `return_sale_items` em `Functions`.
- **Melhoria de metodologia de teste desta sessão:** descoberto que `auth.uid()` (Supabase) lê o GUC `request.jwt.claim.sub`/`request.jwt.claims`, então uma sessão SQL direta pode simular um usuário autenticado com `set local request.jwt.claim.sub = '<uuid>'` antes de chamar uma RPC — isso permite testar de ponta a ponta (incluindo `raise exception` de permissão) sem precisar de um navegador contra o Supabase real. Resolve, para as RPCs desta seção, a limitação registrada nas seções de risco de RF04/RF07 ("não foi possível acionar o `raise exception` real de uma chamada autenticada") — ver validação completa no risco desta seção, abaixo.

### 16. RF13 — correção pós-revisão da PR #5: edição restrita de compra (taxa de câmbio/frete/preços)

- **Problema encontrado (revisão contra o PRD):** `delete_purchase()` (seção 15) bloqueia corretamente a exclusão de uma compra quando parte do estoque dela já foi vendido depois (deixaria o estoque negativo) — mas, com "editar = excluir + relançar" como único caminho de correção (decisão da seção 15), isso deixava essas compras sem nenhuma forma de correção possível.
- **Decisão de escopo (validada com o dono do negócio):** não construir uma tela de edição genérica — excluir + relançar continua sendo o fluxo padrão para vendas, e para compras que não têm esse conflito. A exceção é só para compras, e só para os campos que **não mexem em quantidade/estoque**: taxa de câmbio, frete total, preço unitário em USD e preço de venda. Como esses campos nunca tocam `stock_quantity`, esta correção nunca esbarra na trava de estoque negativo — cobre exatamente o caso que ficava sem saída.
- **`public.update_purchase_pricing(p_purchase_id, p_exchange_rate, p_freight_cost_brl, p_sale_prices)`** (migration `20260910090000_update_purchase_pricing.sql`) — `security invoker`, mesmo racional de `confirm_purchase()`/`delete_purchase()` (`purchases`/`purchase_items` são 100% restritas a master na RLS, não há caso de vendedor). Recalcula, por item, `unit_cost_brl = unit_price_usd × exchange_rate + frete_rateado_por_unidade` — mesma fórmula de `confirm_purchase()` (frete dividido pela quantidade total já lançada na compra, que não muda). **Não** aceita quantidade nem lista de produtos diferente da original: a lista enviada precisa bater exatamente (mesma contagem e mesmos `product_id`) com o que já está em `purchase_items` para aquela compra — tentar adicionar, remover ou trocar um item é bloqueado com `raise exception`, testado nos casos 3 e 4 abaixo.
  - **Não extraímos a fórmula de rateio de frete para uma função auxiliar comum com `confirm_purchase()`:** é uma expressão de uma linha (`freight_cost_brl / quantidade_total`), e essa duplicação já existe, aliás, entre as duas revisões de `confirm_purchase()` na pasta de migrations (seções 11/12) — extrair um helper agora exigiria também tocar em `confirm_purchase()` (já mesclada e testada em produção) só para reusá-lo, adicionando risco sem benefício real para uma linha de código.
  - **Regra mais sutil desta entrega — "compra mais recente do produto":** a correção só atualiza `products.current_unit_cost_brl`/`current_sale_price` (o preço/custo *vigente*, usado em vendas novas) se a compra sendo corrigida ainda for a compra mais recente daquele produto, comparando `p_purchase.purchase_date` com `products.last_purchase_date`. Se uma compra mais nova já tiver sobrescrito o preço vigente, a correção de uma compra mais antiga atualiza só o histórico (`purchase_items` daquela compra) — sem isso, corrigir uma compra antiga poderia "voltar no tempo" um preço que já foi legitimamente atualizado por uma compra posterior, um efeito colateral surpreendente para o master. **Limitação conhecida e aceita:** se duas compras do mesmo produto tiverem exatamente a mesma `purchase_date`, a comparação por igualdade de data não distingue qual delas gravou por último o preço vigente — caso de borda raro (mesmo dia, duas compras do mesmo produto), não tratado nesta entrega.
  - `purchases.updated_at`/`updated_by` são preenchidos automaticamente pelo trigger `purchases_set_updated_meta` (já existente desde a migration inicial, seção "Modelo de dados") — não foi necessário setá-los manualmente na função.
- **Frontend:** `src/components/purchase-correction-panel.tsx` (nova tela `/master/compras/corrigir`, link de entrada em "Registrar compra" e vice-versa) — busca compras por intervalo de data e produto (mesmo padrão de busca de `sale-return-panel.tsx`, seção 15); ao expandir uma compra, mostra taxa de câmbio/frete editáveis e uma linha por item com preço USD e preço de venda editáveis, custo unitário recalculado ao vivo, e um aviso ("há compra mais recente — só atualiza o histórico") quando aquele item não vai refletir no preço vigente do produto, para o master não se surpreender com o resultado.
- `src/integrations/supabase/types.ts` ganhou a assinatura de `update_purchase_pricing` em `Functions`.
- **Gap pré-existente encontrado durante esta revisão (não corrigido agora, fora do pedido desta correção):** `delete_purchase()` (seção 15) não tem nenhum ponto de entrada na UI — a RPC existe desde a PR #5, mas nenhuma tela chama `supabase.rpc("delete_purchase", ...)`. Registrado como pendência abaixo.

### 17. RF08/RF09 — Encomendas (Tela 9) e Relatórios de Giro de Estoque (Tela 10)

- **Sem migração nova:** a tabela `orders` (encomendas) e todas as suas policies de RLS já existiam desde o schema inicial (seção "Modelo de dados") — o schema foi desenhado com esse RF em mente desde o início, então RF08 foi 100% construção de frontend: `src/components/orders-panel.tsx` (cadastro + lista, compartilhado entre master e vendedor) e as rotas `src/routes/master/encomendas.tsx` / `src/routes/vendedor/encomendas.tsx`. O botão "Cadastrar Encomenda" do dashboard do vendedor (que antes só mostrava um toast "Em breve") e os itens de menu "Encomendas"/"Relatórios" do master (antes sem link) foram conectados às telas novas.
  - Cadastro: nome do cliente, contato, produto desejado (select do catálogo ativo via `products_catalog_v`, com uma opção "Outro (não está no catálogo)" que revela um campo de texto livre — cobre a exigência do PRD de aceitar as duas formas) e observação opcional.
  - Lista: por padrão mostra só as encomendas `aberto`, com botão "Marcar como atendido" por linha (`update` direto na tabela, sem RPC — é só uma troca de status, sem cálculo nem regra de negócio envolvida); um toggle simples alterna para ver o histórico de `atendido` (o PRD só pede que os atendidos "fiquem no histórico", não exige uma tela de histórico dedicada, mas manter esse toggle era barato e evita que o master precise consultar o banco diretamente para ver o que já foi resolvido).
  - RLS: `orders_insert_authenticated` exige `created_by = auth.uid()` (preenchido pelo valor default da coluna, não setado manualmente no insert); `orders_select_authenticated`/`orders_update_authenticated` liberam para qualquer usuário autenticado, então a lista e o "marcar como atendido" funcionam para master e vendedor sem diferenciação de papel — igual ao texto do PRD ("Master e vendedor podem marcar uma encomenda como atendida").
- **RF09 — sem RPC/view nova, cálculo no cliente:** `src/components/stock-turnover-report.tsx` (rota `/master/relatorios`, 100% master). Como a RLS já libera `select` completo em `products`/`sales`/`sale_items` para `is_master()`, e esta tela é exclusiva do master, os dados são buscados sem filtro de status/data (histórico completo de `sales`/`sale_items`, mais os produtos ativos de `products_catalog_v`) e o relatório inteiro — "parados", "mais vendidos" e o indicador de giro — é agregado em `useMemo` no frontend, no mesmo espírito de outras telas do master que já filtram/agrupam em JS em vez de RPC dedicada (ex.: `purchase-correction-panel.tsx`, seção 16). Decisão consciente: para o volume de dados de uma loja física pequena (o público-alvo do PRD), isso é suficiente; se o histórico crescer muito, vale revisitar com uma view agregada ou paginação.
  - **"Produto parado" segue exatamente a regra do RF09, independente do filtro de período da tela:** referência = data da última venda do produto (calculada sobre o histórico inteiro, nunca só o período filtrado) ou, se o produto nunca vendeu, `products.last_purchase_date`; o limite de dias vem de `settings.stale_product_days` (não fixado em 30 no código — se o master um dia tiver como mudar esse parâmetro, o relatório já respeita). Isso foi testado deliberadamente com um produto vendido há 45 dias (fora da janela padrão de 30 dias do filtro) para confirmar que ele aparece em "parados" mesmo não aparecendo em "mais vendidos" — os dois cálculos são independentes um do outro, como o PRD exige ("relatório reflete corretamente vendas... dentro do período filtrado" só se aplica a mais vendidos/giro; a regra dos parados é fixa).
  - **Filtro de período (padrão: últimos 30 dias, editável)** se aplica só a "mais vendidos" (soma de `quantity - returned_quantity` por produto dentro do intervalo — quantidade líquida, descontando devoluções já registradas) e ao indicador de giro.
  - **Indicador de giro** — o PRD dá a fórmula só como exemplo ("ex: unidades vendidas ÷ tempo em estoque"), sem especificar o denominador com precisão; interpretamos "tempo em estoque" como dias desde `last_purchase_date` (a entrada mais recente daquele produto no estoque) até hoje, e "unidades vendidas" como a quantidade líquida vendida dentro do período filtrado — decisão registrada aqui por não ser uma fórmula fechada no PRD.
  - "Mais vendidos" usa um gráfico de barra horizontal simples (CSS puro, sem biblioteca de gráficos) em `wine`, como pede a seção 14.5; "parados" é lista simples, ordenada do mais parado ao menos parado, como também pede o PRD.
  - Estado vazio ("Ainda não há dados suficientes para gerar este relatório") aparece quando não há nenhum produto ativo cadastrado; as duas seções também têm um vazio próprio e mais específico ("Nenhuma venda registrada no período selecionado." / "Nenhum produto parado no momento.") para o caso mais comum de já existir catálogo mas faltar histórico de vendas.

### 18. RF13 — correção de causa raiz: nem `delete_purchase()` nem `update_purchase_pricing()` reconstruíam custo/preço do produto

- **Causa raiz (bug confirmado na prática):** uma exclusão manual de compra deixou `products.current_unit_cost_brl`/`current_sale_price` desatualizados no catálogo. Investigando, `delete_purchase()` (seção 15) só revertia `stock_quantity` — nunca tocava custo/preço do produto, mesmo quando a compra excluída era a mais recente dele (ou a única). `update_purchase_pricing()` (seção 16) tinha o problema irmão, mas menos grave: ao corrigir a compra mais recente, o master tinha que redigitar o preço de venda a cada correção (fazia parte do payload) em vez de o sistema recompô-lo.
- **Helper compartilhado — `public.recompute_product_pricing(p_product_id uuid)`** (migration `20260911090000_purchase_pricing_reversal.sql`): dado um produto, olha para a compra mais recente entre as que sobraram em `purchase_items` (ordenando por `purchases.purchase_date desc, purchases.created_at desc` — o desempate por `created_at` resolve, de quebra, a limitação de empate de data que a seção 16 tinha deixado registrada) e recompõe `current_unit_cost_brl` (= `unit_cost_brl` daquela compra), `current_sale_price` (= custo × `(1 + markup_percent/100)`, arredondado) e `last_purchase_date`. Se não sobrar nenhuma compra para o produto, os três campos vão para `null` — não existe custo/preço para sustentar, e "inventar" um valor seria pior do que admitir a ausência.
  - `products.current_unit_cost_brl`/`current_sale_price` deixaram de ser `not null` (`alter table ... drop not null`) para permitir esse estado. `check (col >= 0)` continua valendo (constraints de `check` não rejeitam `null`).
- **`delete_purchase()` corrigida:** antes de excluir, guarda os `product_id` distintos afetados (`array_agg(distinct product_id)`); depois de reverter estoque e excluir a compra (o `on delete cascade` de `purchase_items` já remove os itens dela), chama `recompute_product_pricing()` para cada produto afetado — agora reflete corretamente tanto "sobrou uma compra mais antiga" (custo/preço revertem para ela) quanto "não sobrou nenhuma" (custo/preço viram `null`).
- **`update_purchase_pricing()` — nova assinatura:** `p_items jsonb` (`[{product_id, unit_price_usd}, ...]`) — **não recebe mais `current_sale_price`**. Depois de atualizar `purchase_items`/`purchases` com os novos valores, chama `recompute_product_pricing()` para cada produto do payload — se esta ainda for a compra mais recente dele, `current_sale_price` é recomposto automaticamente (custo novo × markup); senão, a correção fica só no histórico, exatamente como antes, só que agora sem a limitação de empate de data da seção 16 (o helper usa `created_at` como desempate determinístico).
- **Limitação aceita, documentada e validada com o dono do negócio:** o schema guarda histórico de **custo** por compra (`purchase_items.unit_cost_brl`), mas nunca guardou snapshot do **preço de venda confirmado** em cada compra (`current_sale_price` sempre foi sobrescrito, sem histórico). Por isso, ao reverter/corrigir para uma compra anterior, o custo é exato, mas o preço de venda só pode ser **recomposto como sugestão** (custo × markup) — não há como recuperar um ajuste manual de preço que o master tenha feito naquela compra antiga. Isso é aceitável: é a mesma precisão que o próprio fluxo de compra já tem hoje (o master sempre revisa o preço sugerido a cada compra nova).
- **Defesa em profundidade em `confirm_sale()`:** um produto sem `current_sale_price` definido (sem nenhuma compra remanescente) agora é bloqueado com `raise exception` mesmo em chamada direta ao RPC — não só "não aparece para venda" na tela.
- **Frontend — produto sem preço não aparece para venda:** `src/components/sale-form.tsx` (`useActiveProducts`) e `src/routes/vendedor/index.tsx` (`useCatalog`) passaram a filtrar `current_sale_price is not null`, além do `active = true` já existente — um produto que ficou sem custo/preço remanescente simplesmente não aparece nas telas de venda/catálogo do vendedor (a Tela 7 existe para consultar "estoque disponível e preço de venda"; um produto sem preço não serve a esse propósito). `src/routes/master/catalogo/index.tsx` (visão do master, que precisa continuar mostrando o produto mesmo sem preço, para o master saber que precisa comprar de novo) mostra "sem compra" no lugar do valor quando `current_unit_cost_brl`/`current_sale_price` são `null`.
- **`purchase-correction-panel.tsx` ajustado à nova assinatura:** o campo "Preço de venda (R$)" editável foi removido; no lugar, uma coluna somente-leitura "Novo preço sugerido (R$)" mostra o que o backend vai calcular (custo recalculado × `markup_percent` do produto), com o aviso "há compra mais recente — só atualiza o histórico" quando aplicável.
- `src/integrations/supabase/types.ts`: `products.current_unit_cost_brl`/`current_sale_price` (e o espelho em `products_catalog_v`) passaram a `number | null`; `update_purchase_pricing` em `Functions` trocou `p_sale_prices` por `p_items`; `recompute_product_pricing` foi adicionado.

### 26. RF11/RF12 — Fase 2: Despesas Gerais, DRE e Fluxo de Caixa

- **Contexto:** com o V1/MVP validado de ponta a ponta (seção 25), esta entrega inicia a Fase 2 do roadmap (PRD, seção 12): RF11 (Despesas Gerais) e RF12 (DRE + Fluxo de Caixa).
- **Sem migração nova.** A tabela `expenses` e a policy `expenses_all_master` já existiam desde o schema inicial (seção "Modelo de dados", incluídas de propósito para não exigir retrabalho estrutural nesta fase). RF12 não precisou de RPC/view nova: como as duas telas são 100% master, a RLS já existente em `sales`/`sale_items`/`purchase_items`/`expenses` (liberada para `is_master()`) é suficiente, e os cálculos são agregados no cliente — mesmo padrão já usado em `stock-turnover-report.tsx` (RF09).
- **RF11 — `src/components/expenses-panel.tsx`** (rota `/master/despesas`): cadastro (data, categoria — aluguel/taxa de máquina/outras —, valor, descrição opcional) + lista ordenada por data decrescente, mesmo padrão de `orders-panel.tsx` (RF08). O PRD descreve só "lançamento" de despesa, sem edição — foi incluído um botão "Excluir" (via `ConfirmDeleteButton`, já compartilhado com compra/venda/encomenda) como forma mínima de corrigir um lançamento errado, já que a policy `expenses_all_master` cobre `delete` sem nenhuma mudança de RLS.
- **RF12 — CMV por custo médio ponderado, decisão de implementação (substituída na seção 27 — ver abaixo):** a primeira versão calculava o custo médio de cada produto sobre todo o histórico de compras dele até a data de geração do relatório, não restrito ao período filtrado. Essa decisão foi revista logo em seguida por criar um problema de estabilidade (seção 27) — o texto original desta entrada foi superado e não reflete mais o comportamento atual.
- **RF12 — DRE (`src/components/dre-report.tsx`, rota `/master/dre`):** filtro de período (padrão: mês corrente, customizável, conforme "DRE mensal + período customizado"); mostra exatamente as 5 linhas pedidas pelo PRD — Receita de vendas, (–) CMV, (–) Comissões, (–) Despesas gerais, (=) Resultado líquido. `Receita`/`Comissões` vêm de `sales.total_amount`/`commission_amount` já recalculados após eventuais devoluções (RF14), então uma devolução que aconteça depois do período de referência da venda original reduz o resultado do período **da venda**, não do período em que a devolução ocorreu — mesma simplificação já assumida em "mais vendidos" (RF09/`stock-turnover-report.tsx`), que soma `quantity - returned_quantity` sem tentar datar a devolução separadamente; documentado aqui pela mesma razão.
- **RF12 — Fluxo de Caixa (`src/components/cash-flow-report.tsx`, rota `/master/fluxo-caixa`):** extrato cronológico juntando vendas (entrada, por `sale_date`), compras e despesas (saída, por `purchase_date`/`expense_date`) num único período filtrado (mesmo default de mês corrente), com saldo acumulado calculado a partir de **zero no início do período filtrado** — o sistema não guarda (nem o PRD pede) um saldo de caixa anterior ao próprio histórico de lançamentos; se o master quiser o saldo desde o início do negócio, basta abrir o período a partir da primeira compra/venda registrada. Vendas no cartão entram pela própria `sale_date` como entrada integral, seguindo literalmente a premissa já registrada no PRD (seção 11: "vendas no cartão são tratadas como à vista... já que o saldo cai no dia seguinte").
- **Nav:** três itens novos em `src/routes/master/route.tsx` — "Despesas", "DRE", "Fluxo de Caixa" — após "Usuários", fechando a ordem de construção sugerida pelo PRD (seção 6: Fase 2 vem depois de Gestão de Usuários).
- **`EXPENSE_CATEGORY_LABELS` exportado de `expenses-panel.tsx`** e reaproveitado em `cash-flow-report.tsx` (rótulo "Despesa: {categoria}" em cada saída) — evita duplicar o mapeamento categoria→rótulo em dois arquivos; gera um warning inofensivo de `react-refresh/only-export-components` do ESLint (mesmo warning já tolerado em vários componentes `shadcn/ui` do projeto, ex. `button.tsx`/`badge.tsx`, por exportar uma constante junto de um componente no mesmo arquivo).
- **Validação feita nesta sessão**: `tsc --noEmit` limpo; `eslint` sem erros nos arquivos novos/alterados (só o warning de fast-refresh já descrito, mesma categoria já presente em outros arquivos do projeto); `vite build` (produção completa) concluído com sucesso; smoke test SSR de `/master/despesas`, `/master/dre` e `/master/fluxo-caixa` (200, sem crash, sem marcador de erro no corpo da resposta) com o servidor de dev local.
- **Não testado nesta sessão**: o fluxo completo em navegador contra o Supabase real (mesma limitação de rede do sandbox já registrada em todas as entregas anteriores) — em particular, o cálculo de CMV/DRE/fluxo de caixa contra um volume real de compras/vendas/despesas, e o `insert`/`delete` de despesas contra a policy `expenses_all_master` real. Recomenda-se testar no preview da Lovable: lançar algumas despesas de teste, gerar uma compra e uma venda no mesmo mês, e conferir se os totais de Receita/CMV/Comissões/Despesas do DRE e o saldo acumulado do Fluxo de Caixa batem com o cálculo manual esperado.

### 27. RF12 — correção: CMV deixa de usar todo o histórico e passa a cortar no fim do período (PRD atualizado)

- **Contexto:** o PRD (`docs/PRD.md`, seções 7/RF12 e 11) foi atualizado para fechar a ambiguidade que a seção 26 havia resolvido com uma decisão própria: a base do custo médio ponderado do CMV passou a ser explicitamente "todas as compras daquele produto desde o início até a data final do período filtrado (compras posteriores ao período nunca entram no cálculo)", com um novo critério de garantia de estabilidade — "o CMV de um período já fechado não deve mudar ao gerar o relatório novamente no futuro".
- **Problema com a decisão anterior (seção 26):** calcular a média sobre todo o histórico até *hoje* (data de geração do relatório) quebra exatamente essa estabilidade — gerar o DRE de setembro em setembro e gerar de novo em dezembro (depois de novas compras) produzia CMVs diferentes para o mesmo período de setembro, porque compras futuras (em relação a setembro) entravam na média. Isso é uma falha para um relatório financeiro: o resultado de um período fechado não pode mudar retroativamente.
- **Correção em `src/components/dre-report.tsx`:** o custo médio ponderado de cada produto agora considera só as compras com `purchase_date <= fim do período filtrado` (nunca depois). Como essa condição de data vive em `purchases`, não em `purchase_items`, foi adicionado um novo hook `usePurchasesUpToPeriodEnd(dateTo)` (compras filtradas por `purchase_date`) casado por `purchase_id` com `useAllPurchaseItems()` (que passou a selecionar também `purchase_id`) — mesmo padrão de "casar duas consultas por id" já usado para juntar `sale_items` com `sales` no próprio arquivo. A fórmula em si (`Σ(quantidade_do_lote × custo_unitário_do_lote) ÷ quantidade_total_comprada`) não mudou — só o filtro de quais `purchase_items` entram na soma. A quantidade líquida vendida no período (`quantidade − devolvida`) usada para aplicar essa média não mudou.
- **Guard-rail — CMV indefinido:** se um produto aparecer com venda no período mas nenhuma compra registrada até o fim dele (não deveria acontecer dado o controle de estoque, mas o RLS/UI não impede uma inconsistência de dados por completo), o item não entra mais no somatório do CMV como se custasse zero — ele é contado à parte (`productsWithUndefinedCmv`) e um aviso aparece na tela ("N produto(s) foram vendidos no período sem nenhuma compra registrada até o fim dele — o CMV desses itens ficou indefinido e não entrou no total"), mesmo espírito do alerta "produtos sem preço de referência" do RF09 (`stock-turnover-report.tsx`). Sem isso, um bug de dados no CMV inflaria silenciosamente o Resultado Líquido exibido.
- **Fora do escopo desta correção, deliberadamente:** `cash-flow-report.tsx`, `expenses-panel.tsx` e a precificação de compra/venda (RF04/RF06 — `current_unit_cost_brl`/`current_sale_price`, que continuam usando o custo do lote mais recente) não foram tocados — o PRD é explícito que são cálculos com propósitos diferentes sobre a mesma base de `purchase_items`.
- **Validação feita nesta sessão**: `tsc --noEmit` e `eslint` limpos no arquivo alterado (um ajuste de formatação via `eslint --fix`); `vite build` (produção completa) concluído com sucesso; smoke test SSR de `/master/dre` (200, sem crash, sem marcador de erro no corpo da resposta) com o servidor de dev local.
- **Não testado nesta sessão**: o cenário de estabilidade em si (gerar o DRE de um período, registrar uma nova compra do mesmo produto com `purchase_date` posterior ao período, e confirmar que o CMV do período já gerado não muda) contra o Supabase real — mesma limitação de rede do sandbox já registrada em todas as entregas anteriores. Recomenda-se esse teste específico no preview da Lovable, além da validação geral do DRE já pendente da seção 26.

### 28. RF05 — Tela 7 (Estoque), pendência identificada em revisão

- **Contexto:** revisão pós-merge da PR #10 identificou que a Tela 7 (Estoque, PRD seção 6) nunca tinha sido construída — o item "Estoque" no menu do master (`src/routes/master/route.tsx`) era só um placeholder sem rota ("Em breve", em `dashboard-shell.tsx`). O Catálogo (Tela 4) e o Dashboard do vendedor (Tela 3) cobrem parte do propósito, mas não a Tela 7 como especificada.
- **Master — `src/components/stock-panel.tsx`, rota `/master/estoque`:** tabela de consulta rápida (produtos ativos), diferente do Catálogo (que é cadastro/edição, em grade de cards): nome/marca, imagem, ml, quantidade em estoque, preço de venda, custo unitário, margem, data da última compra — nesta ordem, como pede o PRD. Badge "estoque baixo" quando `stock_quantity <= 1`, busca por nome/marca. Sem RPC/view nova: como a tela é 100% master, a RLS já existente em `products` (`products_select_master`) é suficiente; margem é calculada no cliente como `(preço_venda − custo) ÷ custo`, exibida em `%` (ou "—" quando custo/preço estão indefinidos — produto sem nenhuma compra remanescente, mesmo caso já tratado em `products_catalog_v`/RF13 seção 18).
- **Correção pontual (ajuste solicitado após a PR):** a primeira versão desta tela mostrava a coluna de imagem antes do nome do produto — o PRD (seção 6, Tela 7) especifica a ordem "nome do produto, imagem, ml, quantidade, preço...". Corrigida a ordem das colunas em `stock-panel.tsx` (nome primeiro, imagem depois), sem tocar em lógica de dados, cálculo de margem, busca ou badge de estoque baixo.
- **Vendedor — decisão de escopo: não criar uma segunda tela.** O PRD pede, para o vendedor, exatamente "nome do produto, imagem, ml, quantidade disponível, preço de venda final" com busca por nome/marca e badge de estoque baixo — que é **exatamente** o que o Dashboard Vendedor (Tela 3, `src/routes/vendedor/index.tsx`) já entrega desde a fundação do projeto, usando `products_catalog_v` (RF10, sem custo/margem). Construir uma Tela 7 separada para o vendedor duplicaria essa mesma consulta em dois lugares do menu, sem nenhum dado ou filtro adicional — decisão: reaproveitar o Dashboard Vendedor existente como a Tela 7 do papel vendedor, sem nova rota nem novo componente.
- **Nav:** `src/routes/master/route.tsx` — o item "Estoque" deixou de ser placeholder e passou a linkar para `/master/estoque`.
- **Validação feita nesta sessão**: `tsc --noEmit` limpo; `eslint` sem erros no arquivo novo/alterado; `vite build` (produção completa) concluído com sucesso; smoke test SSR de `/master/estoque` (200, sem crash) com o servidor de dev local.
- **Não testado nesta sessão**: o carregamento da tabela contra o Supabase real (mesma limitação de rede do sandbox já registrada em todas as entregas anteriores) — em particular, conferir visualmente a coluna de margem com produtos reais de custo/preço variados, e o comportamento com produto sem nenhuma compra remanescente (custo/preço/margem em "—"). Recomenda-se testar no preview da Lovable.

### 29. RF13 — rede de segurança de integridade de estoque (trigger de banco)

- **Incidente confirmado em produção:** um `DELETE FROM purchases` feito "por fora" de `delete_purchase()` (ex.: SQL editor do Supabase, durante testes/depuração) não reverte `stock_quantity` — só a RPC sabia fazer isso. Isso deixou o estoque de 8 produtos duplicado (órfão), corrigido manualmente. `delete_sale()` tinha o mesmo risco estrutural do lado de vendas.
- **Migration `20260912090000_stock_integrity_triggers.sql`.** Decisão de design: o trigger **não** fica em `purchases`/`sales` (as tabelas-pai, primeiro instinto do pedido original), e sim em `purchase_items`/`sale_items` (as tabelas-filho). Motivo: `purchases`/`sales` já cascateiam (`on delete cascade`) a exclusão dos próprios itens, e um `DELETE` em cascata dispara normalmente os triggers da tabela filha — então um trigger por item cobre **os dois** cenários de risco citados no pedido com uma única fonte de verdade, sem duplicar reversão: (1) excluir a compra/venda inteira por fora da RPC (o incidente real, cascateia para os itens); (2) excluir uma linha de item isolada, sem apagar o registro pai (outro caminho "por fora" possível no SQL editor). Colocar o trigger nas duas pontas causaria dupla reversão exatamente no cenário mais comum (excluir a compra/venda inteira aciona os dois).
- **`reverse_stock_on_purchase_item_delete()`** (`before delete on purchase_items`, `security definer`): decrementa `stock_quantity` em `old.quantity`. `security definer` porque precisa funcionar mesmo quando quem disparou o `DELETE` não tem `UPDATE` em `products` (`products_update_master` é restrita a master) — testado disparando o `DELETE` sem nenhum `auth.uid()` simulado (equivalente a uma sessão de superuser/SQL editor).
- **`reverse_stock_on_sale_item_delete()`** (`before delete on sale_items`, `security definer`): repõe `(old.quantity - old.returned_quantity)`, não `old.quantity` cheio — evita contar em dobro uma devolução já registrada por `return_sale_items()` antes da exclusão. Testado explicitamente: uma venda de 4 unidades com `returned_quantity` ajustado para 1 "por fora" (sem passar por `return_sale_items()`, simulando um dado inconsistente) e depois excluída repôs corretamente 3 unidades, não 4.
- **Rede de segurança contra estoque negativo, sem código extra:** o `check (stock_quantity >= 0)` que já existe em `products` faz esse trabalho sozinho — se a reversão do trigger deixaria o estoque negativo, o `UPDATE` do trigger falha e a exclusão inteira é abortada (testado: um `DELETE FROM purchases` direto, sem RPC, com estoque insuficiente por vendas já feitas, foi rejeitado pelo Postgres com o erro do `check constraint`, e nada ficou parcialmente aplicado).
- **`delete_purchase()`/`delete_sale()` simplificadas:** as duas pararam de reverter estoque manualmente — a reversão passou a ser sempre feita pelo trigger, único lugar que faz essa conta, não importa por onde a exclusão aconteça. `delete_purchase()` manteve uma validação prévia só de leitura (sem `update`) para continuar dando a mensagem amigável, citando o produto específico, antes de tentar o `DELETE` (o `check constraint` seria o bloqueio de fato, mas com uma mensagem genérica do Postgres, pior para quem usa a RPC pela UI). `delete_sale()` manteve a checagem "já tem devolução → bloqueia" (mensagem amigável); o bloqueio estrutural de fato, mesmo por fora da RPC, já existia independentemente disso: `sale_returns.sale_item_id` referencia `sale_items(id)` sem `on delete cascade`, então excluir um item de venda já devolvido sempre falhou (e continua falhando) por violação de FK, com ou sem o trigger novo.
- **Escopo aceito, deliberadamente fora desta entrega:** não foram adicionados triggers de `BEFORE UPDATE` em `quantity`/`returned_quantity` (cotados como "se fizer sentido" no pedido original). Nenhuma RPC hoje corrige `quantity` após o lançamento original (`update_purchase_pricing()` só corrige valores financeiros; `return_sale_items()` só mexe em `returned_quantity`, com sua própria reversão manual de estoque). Adicionar esse trigger exigiria refatorar `return_sale_items()` para não reverter estoque duas vezes — mesma armadilha de dupla reversão que motivou a decisão de design acima —, um passo maior do que o incidente confirmado exige agora. Fica como risco residual conhecido: uma correção manual de `quantity` via SQL direto (não `returned_quantity`, que já tem o guard-rail acima) ainda deixaria o estoque desalinhado sem nenhum aviso.
- **Validação feita nesta sessão — ponta a ponta, contra Postgres real (não apenas leitura de `pg_get_functiondef`):** diferente das sessões anteriores (limitadas pela falta de rede a `*.supabase.co` neste sandbox), desta vez foi possível subir um Postgres 16 local (via `initdb`/`pg_ctl`, sem depender de rede), aplicar a migration completa do projeto (com um schema `auth` mínimo simulando `auth.users`/`auth.uid()`, mesmo padrão de `request.jwt.claim.sub` já usado em sessões anteriores) e rodar cenários reais de ponta a ponta: (1) `DELETE FROM purchases` direto, sem RPC e sem nenhum `auth.uid()` — reproduziu e confirmou a correção exata do incidente relatado (estoque 10→0 corretamente, sem RPC nenhuma envolvida); (2) o mesmo `DELETE` direto quando parte do estoque já tinha sido vendida — bloqueado pelo `check constraint`, nada ficou parcialmente aplicado; (3) `delete_purchase()` pela RPC no cenário de bloqueio — mensagem amigável citando o produto; (4) `delete_purchase()` pela RPC no caminho feliz — estoque revertido **exatamente uma vez** (confirmado comparando o valor esperado; um double-revert teria violado o `check constraint` e o teste teria falhado) e `current_unit_cost_brl`/`current_sale_price` recompostos corretamente para a compra remanescente; (5) `delete_sale()` pela RPC no caminho feliz — estoque restaurado exatamente uma vez; (6) tentativa de `DELETE FROM sales`/`delete_sale()` numa venda com devolução registrada — bloqueada em ambos os casos (RPC pela checagem amigável; `DELETE` direto pela FK de `sale_returns`, independentemente do trigger); (7) `returned_quantity` alterado "por fora" sem `sale_returns` correspondente + exclusão do item isolado (sem apagar a venda) — repôs `(quantity - returned_quantity)` corretamente, não a quantidade cheia; (8) exclusão de um `purchase_item` isolado (sem apagar a compra inteira) — revertido corretamente, e bloqueado corretamente quando deixaria o estoque negativo. Os 8 cenários deram o resultado esperado. O ambiente de teste local foi descartado ao final (nenhum dado ou processo persistente deixado no sandbox).
- **Não testado nesta sessão**: o comportamento contra o schema real do Lovable Cloud/Supabase em produção (extensões, papéis e configurações específicas do serviço gerenciado podem diferir sutilmente do Postgres vanilla usado no teste local) — recomenda-se, ainda assim, uma validação leve no preview da Lovable antes de considerar este item encerrado: registrar uma compra de teste, excluí-la via SQL editor do Supabase (não pela tela), e confirmar visualmente que o estoque do produto reverteu.

### 30. RF03 (PRD v1.1) — recálculo do preço de venda sugerido ao editar o markup (Tela 5)

- **Contexto:** o PRD foi atualizado para v1.1 — a Tela 5 (Cadastro/Edição de Produto) ganhou um novo elemento: ao editar o markup de um produto que já teve ao menos uma compra, o preço de venda sugerido deve ser recalculado em tempo real e confirmável antes de salvar, sem depender de uma nova compra (RF04/Tela 6) para atualizar o `current_sale_price`. Antes desta entrega, editar o markup só valia a partir da próxima compra.
- **`src/components/product-form.tsx`:** o novo campo (`showSuggestedPrice`) só aparece **na edição** de um produto e **apenas** quando `product.current_unit_cost_brl` já está definido (produto com ao menos uma compra registrada) — no cadastro de produto novo, ou na edição de um produto sem nenhuma compra, o campo nunca é exibido e nenhum recálculo acontece, exatamente como pede o PRD.
- **Cálculo e padrão de UI reaproveitado:** `preço_sugerido = current_unit_cost_brl × (1 + markup_%/100)`, arredondado a 2 casas, recalculado a cada tecla digitada no campo de markup. O campo de preço é editável e mostra a sugestão até o master digitar um valor diferente (`salePriceTouched`), replicando exatamente o padrão já usado em `purchase-form.tsx` (linhas `suggestedSalePrice`/`salePriceTouched`) e `purchase-correction-panel.tsx` — mesmo componente de input, mesmo comportamento de "aceitar ou ajustar".
- **Gravação:** nenhuma RPC nova. Ao salvar, o valor final (sugerido ou ajustado) é incluído no mesmo `update` de `products` que já grava os demais campos do formulário — `current_sale_price` muda na mesma transação/chamada do restante da edição, refletindo imediatamente no Catálogo (Tela 4) e no Estoque (Tela 7), sem tocar em `recompute_product_pricing()` (que é based em `purchase_items` e ignoraria um ajuste manual do master, como já documentado na seção 18 — aqui o valor vem direto do formulário, não de uma compra).
- **Validação de formulário:** quando o campo aparece, ele passa a ser obrigatório (`isValid` exige um valor `> 0`, sugerido ou digitado) antes de permitir salvar, e a mensagem de erro de campos obrigatórios passa a mencionar o preço de venda nesse caso.
- **Validação feita nesta sessão**: `tsc --noEmit` limpo; `eslint` sem erros no arquivo alterado (`product-form.tsx`); `vite build` (produção completa) concluído com sucesso. Não houve alteração em `purchase-form.tsx` nem `purchase-correction-panel.tsx` — usados apenas como referência de padrão visual, conforme pedido.
- **Não testado nesta sessão**: o comportamento contra o Supabase/Lovable real (mesma limitação de rede do sandbox já registrada em todas as entregas anteriores) — em particular, confirmar visualmente que o recálculo aparece só para produtos com compra registrada, que o valor gravado em `current_sale_price` aparece corretamente no Catálogo e no Estoque logo após salvar, e que a edição de um produto sem nenhuma compra (`current_unit_cost_brl is null`) realmente não exibe o campo novo. Recomenda-se testar no preview da Lovable antes de considerar este item encerrado.

## Critérios de aceite do PRD atendidos nesta entrega

- RF01: login via Supabase Auth; redirecionamento por papel após login; sem autocadastro público (exceto bootstrap do primeiro usuário, decisão documentada acima).
- RF02 (parcial): tentativa de acesso direto a `/master` por um vendedor (ou vice-versa) é bloqueada e redirecionada; a Tela 11 (gestão de usuários) ainda não foi construída.
- RF10: testado diretamente — `products_catalog_v` não expõe `current_unit_cost_brl`/`markup_percent`; a tabela base `products` só é lida por `is_master()`.
- RNF de auditoria mínima: `created_by`/`created_at` com default automático; `updated_at`/`updated_by` preenchidos via trigger em correções de `purchases`/`sales` (RF13).
- RF03: produto não pode ser salvo sem nome, marca, gênero, ml e markup (validação client-side, com as mesmas colunas `not null`/`check` no banco como garantia); inativar não apaga histórico (RLS/consultas de `purchases`/`sales` continuam funcionando via `product_id`, mesmo com `products.active = false`).
- RF04: ao confirmar a compra, `confirm_purchase()` calcula e grava o custo unitário (com frete rateado por quantidade) e o total em BRL, soma a quantidade ao estoque do produto correspondente, e atualiza `current_sale_price` com o valor confirmado pelo master (sugerido ou ajustado) — tudo em uma única transação.
- RF06: `current_sale_price` é sempre único por produto (uma `update` por produto na mesma transação da compra, nunca dois preços simultâneos); markup configurável por produto usado para a sugestão.
- RF07: venda não pode ser concluída se a quantidade solicitada de algum item (agregada entre linhas do mesmo produto) for maior que o estoque disponível; ao confirmar, `confirm_sale()` debita o estoque de cada item imediatamente, na mesma transação em que grava `sales`/`sale_items`.
- RF06 (comissão): `commission_amount` é calculado automaticamente pelo percentual configurado em `settings` sobre a soma dos subtotais já líquidos de desconto, e nunca é exposto ao vendedor (nem no front, nem no retorno da RPC, que devolve só o id da venda).
- RF13: exclusão de venda repõe a quantidade ao estoque (agregada corretamente por produto); exclusão de compra remove a quantidade do estoque e bloqueia a operação (`raise exception`) se isso deixaria o estoque negativo; vendedor não consegue excluir venda de outro usuário nem fora da janela de 2h (`delete_sale` valida e barra ambos os casos, testado ponta a ponta — ver riscos); master sem limite de tempo, em qualquer venda/compra.
- RF14: após uma devolução, o estoque do produto reflete a quantidade devolvida; `total_amount`/`commission_amount` da venda são reduzidos na mesma proporção da quantidade devolvida; devolver além do saldo disponível naquele item é bloqueado (`raise exception`); venda original não é apagada, só marcada `parcialmente_devolvida`/`totalmente_devolvida`, com histórico em `sale_returns` (o quê, quando, por quem, motivo).
- RF13 (correção complementar): uma compra cuja exclusão está bloqueada por estoque negativo pode ainda ser corrigida em taxa de câmbio/frete/preços via `update_purchase_pricing()`, sem tocar quantidade/estoque; tentar mudar os itens da compra (adicionar, remover ou trocar produto) é bloqueado; o preço/custo vigente do produto só é atualizado se a compra corrigida ainda for a mais recente daquele produto, senão a correção fica só no histórico.
- RF08: vendedor e master cadastram encomenda com nome, contato, produto (catálogo ou texto livre) e observação; ambos podem marcar uma encomenda como atendida; encomendas atendidas saem da lista "em aberto" mas continuam consultáveis no histórico (não são excluídas).
- RF09: relatório mostra "produtos parados" (sem venda há `stale_product_days` dias ou mais, contando da última compra quando o produto nunca vendeu) e "mais vendidos" num período selecionável (padrão 30 dias); testado com um produto vendido fora da janela do filtro para confirmar que a regra dos 30 dias dos "parados" é independente do filtro de período.
- RF13 (causa raiz corrigida, seção 18): excluir uma compra agora recompõe corretamente `current_unit_cost_brl`/`current_sale_price`/`last_purchase_date` do produto a partir da compra remanescente mais recente (ou zera para `null` se não sobrar nenhuma); `update_purchase_pricing()` recompõe o preço de venda automaticamente (custo × markup) em vez de exigir que o master o redigite; um produto sem custo/preço não aparece para venda (front) e é bloqueado por `confirm_sale()` mesmo em chamada direta (backend).
- RF02: master cria novos usuários (papel master ou vendedor) informando nome, e-mail e papel; o novo usuário recebe um convite por e-mail para definir a própria senha (Supabase Auth); a tela lista os usuários existentes com o papel indicado; a criação é bloqueada no servidor para quem não for master (`is_master()`), além de a tela em si só ser alcançável por master (`RequireRole`); e-mail já cadastrado é rejeitado com mensagem clara.
- RF08 (complementar/LGPD): master pode excluir um registro de encomenda a pedido do cliente; a ação não é sequer exibida a um vendedor (a policy `orders_delete_master` já restringia isso no banco desde o schema inicial).
- RF11: master lança despesas com valor, categoria e data; lista de despesas fica disponível para consulta e exclusão.
- RF12: DRE mostra receita de vendas, CMV (custo médio ponderado acumulado até o fim do período filtrado, nunca compras posteriores — seção 27), comissões, despesas gerais e resultado líquido, filtrável por mês corrente ou período customizado; regerar o DRE de um período já fechado produz o mesmo CMV mesmo depois de novas compras serem registradas; Fluxo de Caixa exibe extrato cronológico de entradas (vendas) e saídas (compras, despesas) com saldo acumulado no mesmo período.
- RF05 (seção 28): estoque atualizado automaticamente por compra/venda já era garantido desde RF04/RF07; a Tela 7 de consulta (master: nome, imagem, ml, quantidade, preço, custo, margem, última compra, com badge de estoque baixo; vendedor: mesmos campos sem custo/margem, via Dashboard Vendedor já existente) agora existe em `/master/estoque`, fechando o requisito de visão dedicada de estoque.
- RF13 (seção 29): a reversão de estoque em exclusão de compra/venda passou de responsabilidade só da RPC para uma rede de segurança no banco (trigger em `purchase_items`/`sale_items`) — testado ponta a ponta que uma exclusão feita por fora de `delete_purchase()`/`delete_sale()` (ex.: SQL editor) também reverte o estoque corretamente, sem deixar resíduo, e sem duplicar a reversão quando a exclusão passa pela RPC.
- RF03 (PRD v1.1, seção 30): ao editar o markup de um produto com `current_unit_cost_brl` já definido, o preço de venda sugerido é recalculado em tempo real (custo × novo markup) e exibido em campo editável; o valor confirmado (sugerido ou ajustado pelo master) é gravado em `current_sale_price` no mesmo salvamento do produto, sem exigir uma nova compra; produtos nunca comprados ou em cadastro novo não disparam nenhum recálculo.

## Pendências / próximos passos (ordem sugerida pelo roadmap, seção 6/12)

1. ~~RF02 completo — Tela 11 (gestão de usuários)~~ — implementado na seção 19 (server functions TanStack Start + `supabaseAdmin.auth.admin.inviteUserByEmail`). Com isso, todo o escopo *Must Have* do MVP (seção 5/12 do PRD) tem pelo menos uma versão construída; falta só a rodada de testes reais no preview da Lovable (ver riscos, seção 19) antes de considerar o V1 encerrado.
2. ~~RF03 — imagem antiga órfã no Storage ao trocar a foto de um produto~~ — resolvido na seção 22: `product-form.tsx` remove o arquivo antigo do bucket depois que a troca é salva com sucesso.
3. ~~RF04 — não há tela de histórico/listagem de compras dedicada~~ — resolvido na seção 20: a busca de `purchase-correction-panel.tsx` (por data/produto) já cobre a listagem individual; ganhou também o botão de exclusão (item 6, abaixo).
4. ~~RF07/RF14 — nenhuma listagem completa/paginada de histórico de vendas~~ — resolvido na seção 21: nova tela "Histórico de Vendas" (master, paginada, filtros de vendedor/período/status) e "Minhas vendas" do vendedor deixou de ter um corte fixo em 10, ganhando "Carregar mais".
5. RF13 — decisão de escopo já registrada nas seções 15/16: não existe uma tela dedicada de "edição" de venda (só exclusão), nem de edição de quantidade/produtos de compra — excluir + relançar continua sendo o fluxo para esses casos. Compras têm agora uma exceção pontual (`update_purchase_pricing`, seção 16) para os campos financeiros, que é o único cenário identificado onde "excluir + relançar" ficava sem saída. Se o negócio sentir falta de mais do que isso na prática, vale revisitar depois de rodar o MVP por um tempo.
6. ~~RF13 — `delete_purchase()` sem ponto de entrada na UI~~ — resolvido na seção 20: botão "Excluir compra" em `purchase-correction-panel.tsx`, mesmo padrão do "Excluir venda" já existente em `sale-return-panel.tsx`.
7. ~~RF08 — sem botão de exclusão de encomenda (LGPD)~~ — resolvido na seção 21: botão "Excluir" em `orders-panel.tsx`, visível só para master (a policy `orders_delete_master` já restringia isso no banco).
8. ~~RF04/RF13 — sem aviso proativo para produto sem preço de referência~~ — resolvido na seção 22: alerta "Produtos sem preço de referência" na tela de Relatórios (RF09), com link direto para registrar uma nova compra.

Com os itens 1 a 8 todos resolvidos, não há mais nenhuma pendência conhecida (menor ou maior) registrada neste documento — o que restava era a rodada de testes reais no preview da Lovable contra o Supabase de produção, apontada como "não testado nesta sessão" em praticamente todas as entregas (ver seção de riscos, abaixo), especialmente RF02 (nunca exercida nem parcialmente contra o backend real).

**Atualização (seção 25):** essa rodada de validação real foi concluída pelo dono do negócio no preview da Lovable, com resultado positivo em todos os itens. **O V1 (MVP) está, portanto, encerrado** — sem pendências conhecidas. O próximo passo é a Fase 2 do roadmap (seção 12 do PRD): RF11 (Despesas Gerais) e RF12 (DRE + Fluxo de Caixa).

## Riscos e observações

- **Ambiente de build**: o registro npm privado da Lovable (`europe-west1-npm.pkg.dev`) não é alcançável a partir deste sandbox do Claude Code; a instalação de dependências para validação local foi feita via registro público do npm (não commitado). `bun.lock` continua sendo a fonte da verdade para o pipeline real da Lovable.
- **Validação feita nesta sessão**: `tsc --noEmit` limpo, `eslint` limpo nos arquivos novos/alterados, `vite build` (build de produção completo, incluindo o worker Cloudflare) concluído com sucesso, e a tela de login testada visualmente via Playwright (desktop e mobile) — confirma paleta, tipografia e estados. A função `needs_bootstrap()` foi validada diretamente no banco (retorna `true` com o banco vazio).
- **Não testado**: o fluxo de login/bootstrap ponta a ponta em navegador contra o Supabase real, porque o proxy de rede deste sandbox impediu o Chromium headless de alcançar `*.supabase.co` de forma confiável. Recomenda-se testar esse fluxo no preview da própria Lovable (ou em produção) antes de considerar o RF01 encerrado. A mesma limitação vale para o RF03: o formulário de produto (Tela 5) foi validado visualmente via Playwright (renderização, campos, validação de layout), o redirecionamento de `/master/catalogo` para `/login` quando não autenticado foi confirmado, mas o fluxo completo de criar/editar/inativar um produto (incluindo upload de imagem) contra o banco real não foi exercido neste sandbox — recomenda-se testar no preview da Lovable antes de considerar o RF03 encerrado.
- **RF04 — validação feita nesta sessão**: a função `confirm_purchase()` foi criada e conferida diretamente no banco (`pg_proc.prosecdef = false`, confirmando `security invoker`); a tela (`src/components/purchase-form.tsx`) foi validada visualmente via Playwright, incluindo o cálculo reativo de custo unitário e total da compra a partir de taxa de câmbio/frete/quantidade preenchidos manualmente (ex.: USD 25 × câmbio 5,20 + frete rateado de R$ 3/un. = custo unitário R$ 133,00, conferido na tela). **Não testado nesta sessão**: a chamada real ao RPC contra o Supabase (criação efetiva de uma compra, atualização de estoque/preço de um produto real, e a lista de produtos no seletor, que depende de uma consulta autenticada) — mesma limitação de rede do Chromium headless já registrada acima. Recomenda-se testar uma compra completa (com produto real selecionado) no preview da Lovable antes de considerar o RF04 encerrado.
- **RF04 — correção de duplicidade (seção 12), validação feita nesta sessão**: a checagem `count(*) <> count(distinct product_id)` foi conferida diretamente no banco com um payload de teste (3 itens, 2 `product_id` distintos → detecta a duplicidade corretamente); `pg_get_functiondef()` confirmou que a nova versão da função contém a checagem. No front, o bloqueio foi validado via Playwright com dados de produto simulados (injetados no cache do React Query, contornando a mesma limitação de rede do Chromium headless): selecionar o mesmo produto em duas linhas é rejeitado com a mensagem de erro e o `<Select>` da segunda linha reverte corretamente para "Selecione..." (não fica preso no valor rejeitado); selecionar um produto diferente na segunda linha funciona normalmente e limpa o erro. **Não testado nesta sessão**: o `raise exception` do banco sendo de fato acionado por uma chamada real ao RPC (só a query da checagem foi validada isoladamente) — mesma limitação de rede já registrada acima.
- **RF07 — validação feita nesta sessão**: `confirm_sale()` e `default_commission_percent()` foram criadas e conferidas diretamente no banco (`pg_proc.prosecdef = true` para as duas, confirmando `security definer` como projetado). A tela (`src/components/sale-form.tsx`) foi validada visualmente via Playwright com dados de produto simulados (mesma técnica das seções 12/anteriores — cache do React Query populado diretamente, contornando a limitação de rede do Chromium headless): o cálculo reativo de subtotal por item com desconto e o total da venda foram conferidos (ex.: 2 un. a R$150 com 10% de desconto + 1 un. a R$150 sem desconto = R$420,00, valor exibido confere); a checagem de estoque agregado por produto foi exercida diretamente — duas linhas do mesmo produto somando 4 unidades contra um estoque de 3 mantém o botão "Concluir Venda" desabilitado, e corrigir para 3 unidades habilita o botão novamente.
- **RF07 — correção pós-revisão da PR #4 (seção 14), validação feita nesta sessão**: `pg_get_functiondef()` confirmou que a nova versão de `confirm_sale()` contém a checagem de `discount_percent` e o `status = 'concluida'` explícito no insert; `security definer` seguiu confirmado (`prosecdef = true`) após o `create or replace`. A lógica da condição (`< 0 or > 100`) foi conferida isoladamente com valores de teste (-5, 150, 10) — nenhum falso positivo/negativo. O default pré-existente de `sales.status` foi confirmado via `information_schema.columns` (`column_default = 'concluida'::sale_status`) antes de decidir que nenhum `alter table` era necessário. **Não testado nesta sessão** (resolvido na sessão seguinte — ver risco de RF13/RF14, abaixo, sobre o GUC `request.jwt.claim.sub`): o `raise exception` de desconto inválido sendo de fato acionado por uma chamada real ao RPC autenticada.
- **RF13/RF14 — validação feita nesta sessão**: `pg_get_functiondef()` + `pg_proc.prosecdef` confirmaram a segurança de cada função como projetado (`delete_sale`: `security definer`, `prosecdef = true`; `delete_purchase` e `return_sale_items`: `security invoker`, `prosecdef = false`). **Testado ponta a ponta contra o banco real** (não apenas isoladamente) usando uma técnica nova nesta sessão: `auth.uid()` no Supabase lê o GUC `request.jwt.claim.sub`, então uma transação SQL (sempre revertida com `rollback`, nunca commitada — confirmado depois checando que os dados de teste não persistiram) pode simular um usuário autenticado específico com `set local request.jwt.claim.sub = '<uuid>'` antes de chamar a RPC — isso permite acionar de fato o `raise exception` de permissão/validação de uma chamada autenticada real, e não só validar a lógica isoladamente como nas sessões anteriores. Casos cobertos com dados reais no banco: (1) `delete_sale` pelo vendedor dono, dentro da janela, com 2 linhas do mesmo produto na venda — confirma que o estoque é reposto pela soma correta (não pela última linha, mesmo bug de `confirm_purchase()` da seção 12) e que a venda/itens são apagados; (2) `delete_sale` por vendedor que não é o dono — bloqueado; (3) `delete_sale` pelo dono, mas fora da janela de 2h — bloqueado; (4) `delete_sale` pelo master numa venda já parcialmente devolvida — bloqueado (não pode mais excluir); (5) `delete_purchase` feliz — estoque decrementado corretamente; (6) `delete_purchase` quando reverter deixaria estoque negativo — bloqueado, compra não é excluída, estoque não é alterado (atomicidade confirmada); (7) `delete_purchase` chamado por vendedor — bloqueado; (8) `return_sale_items` chamado por vendedor (Tela 12 é master-only) — bloqueado; (9) devolução parcial (2 de 4 unidades, venda de R$400/comissão R$20) — estoque +2, `total_amount` R$200, `commission_amount` R$10 (proporção da própria venda, 5%), status `parcialmente_devolvida`, linha em `sale_returns` gravada; (10) tentar devolver além do saldo disponível — bloqueado com mensagem informando o saldo real; (11) devolver o restante — `total_amount`/`commission_amount` zerados, status `totalmente_devolvida`. Todos os 11 casos deram o resultado esperado. A tela do master (`sale-return-panel.tsx`) e a lista do vendedor (`seller-recent-sales.tsx`) foram validadas visualmente via Playwright com dados simulados no cache do React Query (mesma técnica das seções anteriores): lista de vendas, expansão de uma venda com itens, preenchimento de quantidade a devolver refletindo corretamente valor a estornar (R$200) e ajuste de comissão (R$10) — os mesmos números do teste de banco (9) acima —, e o botão "Excluir" da lista do vendedor habilitado/desabilitado corretamente conforme janela de tempo e status da venda. **Não testado nesta sessão**: o fluxo completo em navegador contra o Supabase real (mesma limitação de rede do Chromium headless já registrada nas sessões anteriores) — mas, diferente das seções anteriores, a lógica de permissão/negócio das 3 RPCs já foi validada ponta a ponta via chamada autenticada real (não apenas isolada), então o que resta é mais validação de UI/integração do que de regra de negócio.
- **RF08/RF09 (seção 17), validação feita nesta sessão**: como as duas telas não têm nenhuma RPC nova (RF08 usa `insert`/`update` diretos já cobertos pela RLS existente; RF09 é só leitura agregada em JS), a validação foi inteiramente visual, via Playwright, com dados simulados no cache do React Query (mesma técnica das seções anteriores): (1) Encomendas — cadastro com produto do catálogo e com "Outro (não está no catálogo)" (o campo de texto livre aparece só quando essa opção é escolhida), lista "em aberto" com duas encomendas (uma com produto do catálogo, outra com texto livre) e o toggle para "histórico de atendidas" mostrando a encomenda já atendida; (2) Relatórios — três produtos simulados (um nunca vendido, com `last_purchase_date` de 60 dias atrás; um vendido duas vezes dentro dos últimos 30 dias; um vendido uma vez há 45 dias, fora da janela padrão do filtro) confirmaram visualmente que "mais vendidos" mostra só o produto com venda dentro do período (barra proporcional à quantidade), e "produtos parados" mostra os outros dois (60 e 45 dias sem movimento) — inclusive o de 45 dias, que tem venda registrada mas fora da janela do filtro, confirmando que a regra dos "parados" realmente ignora o filtro de período, como pretendido. **Não testado nesta sessão**: o fluxo completo em navegador contra o Supabase real (mesma limitação de rede do Chromium headless já registrada nas seções anteriores) — como não há lógica de banco nova (nenhuma RPC, só RLS já testada em seções anteriores), o risco residual aqui é menor do que nas entregas com RPC nova, mas ainda recomenda-se um teste manual no preview da Lovable, em especial o `insert`/`update` em `orders` (RLS nunca testada ponta a ponta com usuário autenticado real) e a query completa de `sales`/`sale_items` do relatório contra o volume real de dados.
- **RF13 — correção `update_purchase_pricing` (seção 16), validação feita nesta sessão**: `pg_get_functiondef()` + `pg_proc.prosecdef = false` confirmaram `security invoker` como projetado. Testado ponta a ponta contra o banco real com a mesma técnica de `auth.uid()` simulado via `request.jwt.claim.sub` (transação com `rollback`, confirmado depois que nenhum dado de teste persistiu — `select count(*) from products where name like 'Teste RF13%'` retornou 0 após o rollback). Cenários cobertos, todos com o resultado esperado: (1) corrigir uma compra que **não** é mais a mais recente do produto (uma compra mais nova do mesmo produto já existia) — histórico (`purchase_items`/`purchases.total_cost_brl`) atualizado corretamente, mas `products.current_sale_price`/`current_unit_cost_brl` **não** mudaram, continuando com os valores da compra mais nova; (2) corrigir a compra que é a mais recente de dois produtos diferentes (um deles a única compra já feita) — `current_sale_price`/`current_unit_cost_brl` de **ambos** os produtos refletiram a correção; (3) omitir um item de uma compra com 2 produtos na correção — bloqueado (`raise exception`, mensagem citando "mesmos produtos"); (4) trocar um produto por outro que não pertence à compra — bloqueado com a mesma mensagem; (5) chamada por não-master (uid sem linha em `profiles`, logo `is_master()` falso) — bloqueada. A matemática de custo/subtotal também foi conferida nos casos 1 e 2 (ex.: `unit_price_usd` 2.30 × câmbio 6.00 + frete rateado 3.00 = custo unitário R$16,80, subtotal R$168,00 para 10 unidades) e o trigger `purchases_set_updated_meta` confirmado preenchendo `updated_at`/`updated_by` automaticamente. A tela (`purchase-correction-panel.tsx`) foi validada visualmente via Playwright com dados simulados no cache do React Query (mesma técnica das seções anteriores): lista de compras, expansão de uma compra com 2 produtos, recalculo reativo do custo unitário e do total ao editar câmbio/preço USD (ex.: USD 2,30 × câmbio 6,00 = custo unitário R$13,80, novo total R$162,00, conferido na tela), e o aviso "há compra mais recente — só atualiza o histórico" aparecendo exatamente na linha/produto cujo `last_purchase_date` não bate com a data da compra sendo corrigida — em ambos os sentidos: numa compra mais nova cujo produto já foi comprado de novo depois dela (aviso aparece) e numa compra mais antiga com uma compra mais nova do mesmo produto (aviso aparece), confirmando que a lógica do hint no front espelha exatamente a condição do backend. **Não testado nesta sessão**: o fluxo completo em navegador contra o Supabase real (mesma limitação de rede já registrada) — recomenda-se testar uma correção real no preview da Lovable antes de considerar este item encerrado.
- **RF13 — correção de causa raiz (seção 18), validação feita nesta sessão**: `pg_get_functiondef()` + `pg_proc.prosecdef` confirmaram a segurança de cada função (`recompute_product_pricing`, `delete_purchase`, `update_purchase_pricing`: `security invoker`, `prosecdef = false`; `confirm_sale`: `security definer`, `prosecdef = true`, inalterado). `information_schema.columns` confirmou `current_unit_cost_brl`/`current_sale_price` como `is_nullable = YES` após o `alter table`. Testado ponta a ponta contra o banco real com a técnica de `auth.uid()` simulado via `request.jwt.claim.sub`, dentro de um único `do $$ ... $$` terminado em `raise exception` com o resumo dos resultados (garante rollback automático da transação implícita do bloco, sem precisar de `begin`/`rollback` explícitos — confirmado depois checando `select count(*) from products where name like 'ZTESTRF13B%'` = 0). Cenários cobertos, todos com o resultado esperado: (1) `delete_purchase()` da **única** compra de um produto — `current_unit_cost_brl`/`current_sale_price`/`last_purchase_date` viram `null`, estoque zera, e uma tentativa de `confirm_sale()` para esse produto é bloqueada com a mensagem "está sem preço de venda definido"; (2) `delete_purchase()` da compra **mais recente** de um produto que tem uma compra mais antiga remanescente — custo/preço revertem corretamente para os valores dessa compra mais antiga; (3) `delete_purchase()` da compra **mais antiga** de um produto (não é a mais recente) — custo/preço do produto não mudam; (4) `update_purchase_pricing()` bem-sucedido recalculando custo/preço quando é a compra mais recente do produto (conferido também com a matemática: USD 12,00 × câmbio 6,00 + frete rateado R$4,00/un. = custo R$76,00, preço sugerido R$76,00 × 1,30 = R$98,80, subtotal R$760,00 para 10 unidades — todos batendo); (5) `update_purchase_pricing()` de uma compra que não é mais a mais recente — só o histórico (`purchase_items`) muda, `current_sale_price`/`current_unit_cost_brl` do produto continuam os da compra mais nova; (6) `update_purchase_pricing()` tentando trocar o produto de um item — bloqueado (`raise exception`, mensagem citando "mesmos produtos"); (7) `delete_purchase()` chamado por não-master — bloqueado; (8) `update_purchase_pricing()` chamado por não-master — bloqueado. **Não testado nesta sessão**: o fluxo completo em navegador contra o Supabase real (mesma limitação de rede já registrada) — a tela `purchase-correction-panel.tsx` foi só ajustada (removido o campo de preço de venda editável, adicionada a coluna "novo preço sugerido") e revalidada visualmente via Playwright com dados simulados no cache do React Query (recalculo ao vivo do custo e da sugestão de preço ao editar taxa de câmbio/preço USD), mas não a chamada real ao RPC nem o comportamento de `sale-form.tsx`/dashboard do vendedor filtrando produtos sem preço contra dados reais.

### 19. RF02 — Gestão de Usuários (Tela 11)

- **Sem migração nova.** O schema já suportava isso desde a fundação (seção 5): o trigger `handle_new_user()` (migration inicial) já cria a linha em `profiles` com o `role` vindo de `raw_user_meta_data.role`, exatamente pensando neste RF ("uso futuro: criação de usuários pelo master via função administrativa, RF02/Tela 11" — comentário já presente na migration desde a sessão 1). Esta entrega só precisava do lado que faltava: uma forma de criar o usuário em `auth.users` com esse metadata, o que exige a Admin API (`service role`), inacessível ao client.
- **Decisão de arquitetura — servidor via TanStack Start, não Supabase Edge Function:** o projeto já é uma app TanStack Start (SSR) com a infraestrutura de server functions pronta desde o scaffold da Lovable — `src/integrations/supabase/client.server.ts` (client `service role`, importável só em código server-only), `src/integrations/supabase/auth-middleware.ts` (`requireSupabaseAuth`, valida o Bearer token do usuário logado e expõe `context.supabase`/`context.userId` autenticados como ele) e `src/start.ts` (registra `attachSupabaseAuth` globalmente, então toda chamada de server function já sai do browser com o token do usuário, sem código extra por tela). Como isso já existia e cobre exatamente esse caso de uso, criar uma Supabase Edge Function separada (Deno, deploy próprio) seria uma segunda infraestrutura fazendo o mesmo trabalho — não foi necessário.
- **`src/lib/manage-users.functions.ts`** — duas server functions (`createServerFn`), primeira vez que o projeto usa esse mecanismo (até aqui, toda regra de negócio sensível vivia em RPC de banco — `confirm_purchase`, `confirm_sale`, etc. — porque bastava SQL/RLS; a Admin API de Auth só existe no SDK do `supabase-js`, não em SQL, por isso a necessidade de um servidor):
  - `assertMaster()` — chama `supabase.rpc("is_master")` (mesma função já usada em todas as policies de RLS desde a migration inicial) usando o client autenticado do middleware; mesmo padrão de defesa em profundidade já usado nas RPCs de banco (a Tela 11 já é inacessível a um vendedor pelo `RequireRole` do layout `/master`, mas a função repete a checagem porque ela por si só chama `supabaseAdmin`, que ignora toda RLS).
  - `listManagedUsers()` (GET) — lê `profiles` (nome, papel, data de criação) com o client autenticado (respeitando a RLS existente, que já libera "todas as linhas" para `is_master()`), e complementa com o e-mail de cada usuário via `supabaseAdmin.auth.admin.listUsers()` — e-mail vive em `auth.users`, uma tabela do schema interno do Supabase que nunca é exposta por PostgREST/RLS, só pela Admin API.
  - `createManagedUser({ name, email, role })` (POST, validado com `zod`) — chama `supabaseAdmin.auth.admin.inviteUserByEmail(email, { data: { name, role } })`. Optou-se por **convite** (link por e-mail para o próprio usuário definir a senha) em vez de gerar uma senha inicial no servidor, seguindo literalmente o texto da Tela 11 no PRD ("Envio de convite/senha inicial via Supabase Auth, e-mail de confirmação") — o novo usuário nunca fica sabendo de uma senha temporária que o master teria que repassar por fora do sistema. O `data` do convite carrega `name`/`role`, que o trigger `handle_new_user()` já sabe consumir de `raw_user_meta_data` sem nenhuma mudança nele.
  - Import de `supabaseAdmin` é **dinâmico** (`await import(...)`) dentro do corpo dos handlers, nunca no topo do módulo — repete o aviso já deixado em `client.server.ts` ("route files and `*.functions.ts` ship to the client bundle"): o arquivo que declara a server function é, ele mesmo, enviado ao bundle do browser (o compilador do TanStack Start extrai só o código do lado certo antes de montar cada bundle), então um import estático do client de service role no topo arriscaria vazá-lo para o cliente.
  - Erro de e-mail duplicado (`auth.admin.inviteUserByEmail` retorna erro do Supabase quando o e-mail já existe) é traduzido para "Este e-mail já está cadastrado." — critério de aceite explícito da Tela 11 (estados de erro, seção 6).
- **Frontend:** `src/components/users-panel.tsx` (formulário "Novo usuário": nome, e-mail, papel — master ou vendedor — mesmo `<Select>` dos demais formulários do projeto; lista de usuários existentes com papel indicado) e a rota `src/routes/master/usuarios.tsx`, seguindo o mesmo padrão de `orders-panel.tsx`/`src/routes/master/encomendas.tsx` (painel + rota fina, React Query para cache/invalidação). O item de nav "Usuários" (`src/routes/master/route.tsx`), até agora um placeholder "Em breve", passou a apontar para `/master/usuarios` — RF02 fecha o último Must Have do MVP (seção 5/12 do PRD) sem tela própria construída.
- **Validação feita nesta sessão**: `tsc --noEmit` limpo, `eslint` limpo nos arquivos novos/alterados, `vite build` (build de produção completo) concluído com sucesso — confirmando, entre outras coisas, que `manage-users.functions.ts` foi corretamente dividido pelo compilador em um chunk de servidor (`_ssr/manage-users.functions-*.mjs`) sem vazar `supabaseAdmin`/a service role key para o bundle do cliente. As duas rotas novas (`/master/usuarios`) e alteradas responderam HTTP 200 via SSR num smoke test com o servidor de dev rodando localmente (sem crash de import/render).
- **Não testado nesta sessão** (mesma limitação de rede já registrada em todas as entregas anteriores, agora também no lado servidor): o fluxo completo em navegador contra o Supabase real. Diferente das telas anteriores — cuja regra de negócio vive em RPC de banco e cuja UI pôde ser validada visualmente populando o cache do React Query com dados simulados, contornando por completo a rede — esta entrega tem uma camada nova (`requireSupabaseAuth`, que valida o token do usuário contra o serviço de Auth do Supabase) rodando no **servidor** da própria aplicação, fora do alcance do Playwright/cache do React Query: não há como simular de forma realista, neste sandbox, uma sessão autenticada chegando ao servidor de dev e sendo validada pelo Supabase real. Recomenda-se testar esta tela de ponta a ponta (criar um vendedor de teste, conferir o e-mail de convite chegando, listar usuários) no preview da própria Lovable antes de considerar o RF02 encerrado — é a validação mais importante pendente do MVP inteiro, por ser a única tela ainda não exercida nem parcialmente contra o backend real.
- **Dependência de infraestrutura, fora do controle do código:** `inviteUserByEmail` só efetivamente entrega o e-mail se o projeto Supabase/Lovable Cloud tiver um provedor de e-mail configurado (SMTP custom ou o remetente padrão do Supabase, que tem limites baixos de envio). Isso é configuração do projeto, não algo que a aplicação controle — vale confirmar no painel antes do primeiro uso real em produção.

### 20. RF04/RF13 — botão "Excluir compra" em `purchase-correction-panel.tsx`

- **Sem migração nova.** `delete_purchase()` já existia desde a seção 15 (revisado na 18) — reverte o estoque item a item e bloqueia (`raise exception`) se isso deixaria algum produto negativo, e recompõe `current_unit_cost_brl`/`current_sale_price`/`last_purchase_date` do produto a partir da compra remanescente mais recente. Faltava só um botão na UI chamando essa RPC — pendência registrada nas seções 16/18 do próprio `PROGRESSO.md`.
- **Decisão consciente de reaproveitar a tela existente, não criar uma nova:** `purchase-correction-panel.tsx` já busca compras por intervalo de data e produto (a mesma pendência de RF04 — "não há listagem de compras dedicada" — descrevia exatamente essa busca, só que sem falar nela por esse nome). Criar uma segunda tela de listagem só para o botão de excluir duplicaria as mesmas queries (`usePurchases`/`useItemsByPurchase`/`useProducts`) já existentes ali. Por isso a solução foi: adicionar `handleDelete()` ao `PurchaseRow` (mesmo padrão de `handleDelete()` em `sale-return-panel.tsx`, incluindo variant/estilo do botão) e renomear a tela de "Corrigir compra" para "Compras registradas" (`src/routes/master/compras/corrigir.tsx`) — o nome da rota (`/master/compras/corrigir`) não mudou, só o título visível e a descrição da página, para não quebrar o link cruzado já existente em `nova.tsx`. Isso resolve as duas pendências (RF04 item 3 e RF13 item 6) com uma mudança só.
- **Diferença deliberada em relação ao botão "Excluir venda":** `sale-return-panel.tsx` desabilita o botão de exclusão com `canDelete = sale.status === "concluida"` porque dá para saber, só olhando a linha carregada, se a venda já foi devolvida (campo `status`). Compras não têm um campo equivalente — saber se a exclusão vai ser bloqueada exigiria repetir no cliente a mesma verificação de estoque negativo que o backend já faz (`recompute` por produto, considerando vendas depois da compra). Não valeu a pena duplicar essa lógica só para desabilitar o botão preventivamente: ele fica sempre habilitado, e a mensagem de erro do `raise exception` (já testada ponta a ponta na seção 15/18) aparece normalmente se a exclusão for bloqueada.
- **Frontend:** `src/components/purchase-correction-panel.tsx` — `PurchaseRow` ganhou `deleting`/`handleDelete()` e um botão "Excluir compra" no rodapé do item expandido (`variant="ghost"`, `text-danger`), chamando `supabase.rpc("delete_purchase", { p_purchase_id: purchase.id })` e reusando o `onMutated()` (`invalidateAfterMutation`) já existente para atualizar catálogo/estoque/dashboard depois. `src/routes/master/compras/corrigir.tsx` e `.../nova.tsx` tiveram só texto ajustado (título e os dois links cruzados) para refletir que a tela agora também exclui, não só corrige.
- **Validação feita nesta sessão**: `tsc --noEmit` e `eslint` limpos nos arquivos alterados; `vite build` (produção completa) concluído com sucesso; smoke test SSR de `/master/compras/corrigir` e `/master/compras/nova` (200, sem crash) com o servidor de dev local. **Não testado nesta sessão**: a chamada real ao RPC contra o Supabase (mesma limitação de rede do Chromium headless já registrada em todas as entregas anteriores) — como `delete_purchase()` em si já foi validado ponta a ponta contra o banco real nas seções 15/18 (incluindo o caso de bloqueio por estoque negativo), o risco residual aqui é só de UI/integração (o botão realmente disparar a RPC e o `invalidateQueries` atualizar a tela), não de regra de negócio.

### 21. RF08 — exclusão de encomenda (LGPD) e RF07/RF14 — histórico de vendas paginado

- **Sem migração nova em nenhuma das duas.** A policy `orders_delete_master` já existia desde o schema inicial (restringindo `delete` em `orders` ao master); e a listagem paginada é 100% leitura sobre `sales`/`sales_seller_v`, já cobertas por RLS existente.
- **RF08 — `src/components/orders-panel.tsx`:** `OrdersList` passou a ler o papel do usuário logado (`useAuth()`) e mostra um botão "Excluir" (variant `ghost`, `text-danger`, mesmo padrão dos outros botões destrutivos do projeto) só quando `profile.role === "master"` — a coluna de ações, antes exclusiva do status `aberto` (botão "Marcar como atendido"), agora aparece também para o master no histórico de atendidas, com os dois botões lado a lado quando fizer sentido. Não há confirmação extra (`window.confirm`) antes de excluir, seguindo o mesmo padrão já usado em "Excluir venda"/"Excluir compra" (a ação já exige um clique deliberado). O vendedor nunca vê o botão — nem chega a ser barrado pela RLS, porque a opção simplesmente não é renderizada para ele.
- **RF07/RF14 — duas mudanças complementares**, uma por papel:
  - **Master — nova tela "Histórico de Vendas"** (`src/components/sales-history-panel.tsx` + rota `src/routes/master/vendas/historico.tsx`, nav item novo em `master/route.tsx`): listagem só-leitura de todas as vendas, com filtros de vendedor/período/status e paginação via `useInfiniteQuery` (`range()` de 20 em 20, botão "Carregar mais"). Deliberadamente **sem** ação de corrigir/devolver/excluir — isso continua sendo trabalho da tela "Devolução de Venda" (`sale-return-panel.tsx`), que por sua vez ficou limitada a 50 resultados por ser uma tela de busca-para-agir, não de navegação de histórico. As duas telas agora se cruzam por link (mesmo padrão de `compras/nova` ↔ `compras/corrigir`, seção 20), deixando claro qual usar para cada objetivo.
  - **Vendedor — `seller-recent-sales.tsx`** trocou o `.limit(10)` fixo por `useInfiniteQuery` com o mesmo padrão de paginação (`range()`/"Carregar mais", páginas de 10), então "Minhas últimas vendas" virou "Minhas vendas": o vendedor já não fica limitado a olhar só as últimas 10 se quiser conferir uma venda mais antiga (dentro do que a RLS de `sales_seller_v` já permite, isto é, as próprias vendas).
  - Optou-se por `useInfiniteQuery`/"Carregar mais" em vez de paginação numerada (página 1, 2, 3...) nas duas telas: para o volume do negócio (≈50 vendas/mês, por hipótese do PRD, seção 8), uma lista que cresce sob demanda é mais simples de implementar e de usar no celular do que uma paginação com números de página, sem abrir mão de eventualmente rolar por todo o histórico.
- **Validação feita nesta sessão**: `tsc --noEmit` e `eslint` limpos nos arquivos novos/alterados; `vite build` (produção completa) concluído com sucesso; smoke test SSR de `/master/vendas/historico`, `/master/vendas/devolucao`, `/master/encomendas`, `/vendedor/encomendas` e `/vendedor` (200, sem crash) com o servidor de dev local. **Não testado nesta sessão**: o fluxo completo em navegador contra o Supabase real (mesma limitação de rede já registrada em todas as entregas anteriores) — em particular, a paginação (`useInfiniteQuery`/`range()`) e o filtro de status da nova tela de histórico não foram exercidos contra um volume real de vendas, e o botão de exclusão de encomenda não foi acionado contra a policy `orders_delete_master` real. Recomenda-se testar as duas entregas no preview da Lovable, criando mais de 20 vendas de teste para exercitar de fato o "Carregar mais".

### 22. RF03 — limpeza de imagem órfã no Storage e RF04/RF13 — alerta de produto sem preço

- **Sem migração nova em nenhuma das duas** — as duas eram lacunas de frontend sobre comportamento já existente no banco/Storage.
- **RF03 — `src/components/product-form.tsx`:** o path de upload de imagem sempre incluiu um timestamp (`${id}/${Date.now()}.${ext}`, desde a fundação do RF03 — seção 10), então trocar a foto de um produto na edição nunca sobrescrevia o arquivo anterior no bucket `products`, só o arquivo novo ficava valendo em `image_url` — o antigo ficava para sempre no Storage, sem nenhuma referência. A correção:
  - Novo helper `extractStoragePath(publicUrl, bucket)` — a API do Storage só aceita remover por path relativo (`{id}/{timestamp}.{ext}`), não pela URL pública inteira (`.../storage/v1/object/public/products/{id}/{timestamp}.{ext}`), então a função extrai a parte depois de `/object/public/{bucket}/`.
  - `handleSubmit()` guarda `previousImageUrl` antes de decidir o novo `imageUrl`, e só chama `supabase.storage.from("products").remove([previousPath])` **depois** que o `update`/`insert` do produto já foi salvo com sucesso — nessa ordem deliberada: se a exclusão viesse antes e o salvamento do produto falhasse por qualquer motivo, o produto ficaria sem nenhuma imagem válida (nem a antiga, já apagada, nem a nova, não confirmada). Um arquivo órfão remanescente é um problema bem mais barato do que isso.
  - A remoção é *best-effort*: se falhar (`removeError`), só um `console.warn`, nunca um erro exibido ao master — o produto já foi salvo com a imagem nova corretamente, então não há por que interromper o fluxo por causa de uma limpeza de bucket que pode ser refeita manualmente depois.
- **RF04/RF13 — `src/components/stock-turnover-report.tsx`:** a seção 18 já tinha resolvido o problema de fundo (produto sem `current_unit_cost_brl`/`current_sale_price` some das telas de venda e é bloqueado por `confirm_sale()`), mas o master só descobria isso olhando o Catálogo produto a produto. A tela de Relatórios (RF09, já visitada regularmente para decidir compras) ganhou uma nova seção condicional "Produtos sem preço de referência" — aparece só quando existe pelo menos um produto ativo com `current_sale_price is null` (`products_catalog_v`, que já expõe essa coluna desde a fundação, RF10) — listando os produtos afetados com um link direto para "Registrar compra" (`/master/compras/nova`). Estilo `warning` (`bg-warning/10`/`text-warning`), mesmo padrão de badge de estoque baixo já usado no catálogo e no dashboard — reservado a alertas operacionais, nunca `wine`/`amber` (identidade de marca), conforme a diretriz da seção 14.2 do PRD.
- **Validação feita nesta sessão**: `tsc --noEmit` e `eslint` limpos (incluindo autofix de formatação do Prettier num parágrafo do alerta); `vite build` (produção completa) concluído com sucesso; smoke test SSR de `/master/relatorios` e `/master/catalogo/novo` (200, sem crash) com o servidor de dev local. **Não testado nesta sessão**: o fluxo completo em navegador contra o Supabase real (mesma limitação de rede já registrada em todas as entregas anteriores) — em particular, a remoção efetiva de um arquivo do bucket `products` via `storage.remove()` e o alerta de Relatórios aparecendo/desaparecendo corretamente conforme produtos reais ficam com ou sem `current_sale_price`. Recomenda-se testar no preview da Lovable: editar a imagem de um produto duas vezes seguidas e conferir no painel do Supabase que só o arquivo mais recente permanece no bucket; e excluir a única compra de um produto de teste para conferir o alerta aparecendo em Relatórios.

### 23. Confirmação antes de excluir compra/venda/encomenda (ajuste solicitado após a PR)

- **Contexto:** os quatro botões de exclusão do sistema (`purchase-correction-panel.tsx`/"Excluir compra", `sale-return-panel.tsx`/"Excluir venda", `seller-recent-sales.tsx`/"Excluir" no dashboard do vendedor, `orders-panel.tsx`/"Excluir" em encomendas) disparavam a RPC/delete direto no primeiro clique, sem nenhuma confirmação — risco de exclusão acidental num toque, especialmente no celular (uso principal do vendedor, seção 8/RNF de responsividade do PRD).
- **`src/components/confirm-delete-button.tsx`** (novo) — componente compartilhado que envolve o `AlertDialog` do shadcn/ui (`src/components/ui/alert-dialog.tsx`, já existente no projeto mas nunca usado até agora) num botão só: quem chama continua dono do próprio estado de carregamento (`pending`/`disabled`) e do handler real (`onConfirm`) — o componente só interpõe "tem certeza?" antes de invocar esse handler, sem duplicar a lógica de exclusão em si em cada tela. Criado como componente novo (em vez de repetir o `AlertDialog` inteiro nos 4 arquivos) porque a interação é idêntica nos quatro casos — mesmo racional de outras abstrações já existentes no projeto (`DashboardShell`, `RequireRole`).
  - Usa o botão com `variant="destructive"` do shadcn (`buttonVariants({ variant: "destructive" })`) como ação de confirmar — que já mapeia para o token `danger` do design system (`--destructive: var(--danger)`, `src/styles.css`), então nenhuma cor nova foi introduzida.
  - Suporta um `triggerTitle` opcional (o `title` HTML do botão-gatilho, usado por `sale-return-panel.tsx`/`seller-recent-sales.tsx` para explicar por que o botão está desabilitado — ex: "fora da janela de correção"), sem overloadar a prop `title` do diálogo (que já é o título da pergunta de confirmação).
- **Mensagens de confirmação, uma por contexto** (não um texto genérico "Tem certeza?"): cada diálogo explica o efeito concreto da exclusão — "o estoque comprado nela será revertido" (compra), "o estoque vendido nela será reposto" (venda, nas duas telas onde existe: master e vendedor), "o registro do pedido será apagado definitivamente" (encomenda) — e todas terminam com "Esta ação não pode ser desfeita", já que nenhuma das quatro tem uma forma de desfazer depois de confirmada.
- **Nada mudou na lógica de negócio:** os quatro `handleDelete()` continuam exatamente como estavam (mesmas RPCs, mesmo `invalidateQueries`, mesmo tratamento de erro — banner inline em compra/venda, `toast.error` em encomenda/vendedor) — só passaram a ser chamados a partir do `onConfirm` do diálogo em vez de diretamente do `onClick` do botão.
- **Validação feita nesta sessão**: `tsc --noEmit` e `eslint` limpos nos arquivos alterados (com um ajuste de tipo — `triggerTitle?: string | undefined`, necessário porque o projeto roda com `exactOptionalPropertyTypes: true` e os dois call sites passam `undefined` explicitamente na expressão condicional, não omitem a prop); `vite build` (produção completa) concluído com sucesso. Testado visualmente via Playwright numa rota temporária isolada (criada e removida só para este teste, nunca commitada) renderizando `ConfirmDeleteButton` sozinho, sem depender de autenticação: abrir o diálogo mostra título/descrição/botões corretos, "Cancelar" fecha sem disparar `onConfirm`, nenhum erro de console. **Não testado nesta sessão**: o fluxo real de clicar em "Confirmar exclusão" de fato disparando a RPC no navegador contra o Supabase (mesma limitação de rede já registrada em todas as entregas anteriores) — mas como a lógica de exclusão em si não mudou (só passou a ser chamada por um caminho diferente), o risco residual é só de o diálogo abrir/fechar corretamente na integração real, já coberto pelo teste isolado acima.

### 24. Layout — remove teto de largura do DashboardShell (ajuste solicitado após a PR)

- **Problema:** o wrapper de nav+conteúdo em `src/components/dashboard-shell.tsx` usava `mx-auto flex max-w-6xl ...`, limitando a área útil a 1152px e centralizando o conteúdo — em telas mais largas sobrava espaço em branco nas laterais, perceptível especialmente em telas operacionais com tabela (ex: Registrar Compra, Histórico de Vendas).
- **Correção:** removidos `mx-auto` e `max-w-6xl` da `<div>` que envolve nav+`<main>`, mantendo intacto o padding lateral já existente (`px-4 sm:px-6`) e todo o resto (`flex flex-col gap-6 py-6 lg:flex-row`). Mudança de uma linha, só no teto de largura — nada de cor, tipografia ou navegação foi tocado, como pedido. Como `DashboardShell` é o layout compartilhado por todas as telas de master e vendedor, o efeito é automático em todas elas (Catálogo, Compras, Estoque, Vendas, Encomendas, Relatórios, Usuários), sem precisar tocar em cada rota.
- **Validação feita nesta sessão**: `tsc --noEmit` e `eslint` limpos; `vite build` (produção completa) concluído com sucesso. Testado visualmente via Playwright numa rota temporária isolada (criada e removida só para este teste, nunca commitada) renderizando `DashboardShell` com uma tabela de exemplo, num viewport de 1920px: antes da mudança o conteúdo ficaria preso a 1152px centralizados; depois, o `<main>` ocupa a largura toda disponível (medido via `boundingBox()`: 1656px de largura numa viewport de 1920px, descontando só a coluna de nav e o padding lateral). **Não testado nesta sessão**: o efeito em telas reais do app (Registrar Compra, Histórico de Vendas) contra o Supabase real, mesma limitação de rede do sandbox já registrada — mas como a mudança é puramente estrutural (CSS do container, sem lógica), o teste isolado do componente já cobre o risco.

### 25. Validação real no preview da Lovable — V1 (MVP) encerrado

- **Contexto:** todas as entregas anteriores (seções 1 a 24) foram validadas neste sandbox só com `tsc`/`eslint`/`vite build` limpos e testes visuais via Playwright com dados simulados no cache do React Query — o proxy de rede do ambiente de desenvolvimento impedia o Chromium headless de alcançar `*.supabase.co`, então nenhuma tela tinha sido exercida de ponta a ponta contra o Supabase/Lovable Cloud real. A pendência final registrada era exatamente essa rodada de testes reais, na seguinte ordem sugerida: (1) RF02 — Gestão de Usuários; (2) RF01/RF03 — login/bootstrap e cadastro/edição de produto com imagem; (3) RF04/RF07 — compra e venda completas; (4) RF13/RF14 — exclusão de compra/venda e devolução; (5) RF08/RF09 — encomendas e relatório de giro.
- **Resultado:** o dono do negócio validou manualmente os itens 1 a 5 no preview real da Lovable (contra o Lovable Cloud/Supabase de produção do projeto) e confirmou que tudo funcionou conforme esperado — sem divergências reportadas em relação aos critérios de aceite do PRD (seção 7).
- **Conclusão:** com isso, todo o escopo *Must Have* — V1/MVP (PRD, seções 5 e 12) está implementado **e** validado de ponta a ponta contra o ambiente real, não só testado isoladamente/simulado. Não há mais nenhuma pendência conhecida de V1 neste documento.
- **Próximo passo, pelo roadmap do próprio PRD (seção 12):** a Fase 2 (V1.1 — RF11/RF12: Despesas Gerais, DRE, Fluxo de Caixa) é a próxima entrega no MoSCoW (🟡 Should Have), condicionada pelo PRD a "V1 estar em uso real por pelo menos algumas semanas, para gerar dados confiáveis" (seção 12) — ou seja, o código da Fase 2 pode começar a ser construído a qualquer momento, mas os relatórios (DRE/fluxo de caixa) só passam a fazer sentido depois de um período de uso real gerando dados de compras/vendas/despesas.
